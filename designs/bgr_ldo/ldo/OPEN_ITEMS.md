# OPEN_ITEMS.md — 미해결 항목 레지스터 (LDO)

최종 갱신: 2026-07 rev2 — D6 종결, 전파 규약 추가
위치: `designs/bgr_ldo/ldo/OPEN_ITEMS.md`

> **이 문서의 존재 이유**
> `A1`, `D4`, `E1` 같은 식별자가 어느 문서에도 정의되지 않은 채 채팅 사이를 돌아다녔다.
> 정의를 가진 사람의 컨텍스트가 끝나면 라벨은 의미 없는 문자열이 된다 —
> 이 프로젝트가 반복해서 사고를 낸 **"암묵적 상태 전달"** 의 전형이다.
> 이 문서가 라벨의 **유일한 정본**이다.

> **`DECISIONS.md`와의 분업**
> `DECISIONS.md` = **판정이 끝난 것**의 근거·대체 이력 (D-001 …).
> 이 문서 = **아직 안 끝난 것** + 그 해소 경로.
> 해소되면 한 줄 요약만 남기고 상세는 `DECISIONS.md` 또는 해당 스펙 문서로 이관한다.

---

## 0. 라벨 체계

| 군 | 뜻 | 해소 수단 |
| :---: | :--- | :--- |
| **A** | **출처 불명 수치** — 조건 병기가 없어 규율 5 위반 상태 | 1차 출처 추적, 없으면 폐기 선언 |
| **B** | **문서 간 모순** | 사용자 정본 결정 |
| **C** | **LUT로 답할 수 있는 것** | `lut/lookup.py` 조회 |
| **D** | **ngspice 실측 / PDK 파일 확인이 필요한 것** | 시뮬·grep (사용자 실행) |
| **E** | **조기 리스크** — Task 순서상 나중이지만 지금 판정해야 앞이 안 뒤집히는 것 | 사안별 |

번호는 군 안에서 **일련 증가, 재사용 금지.** 폐기된 항목도 번호를 비우지 않는다.
**상태**: `OPEN` / `PARTIAL` / `CLOSED` / `VOID`(폐기)

---

## 0.5. ★ 이 문서를 쓰는 규약 (전 채팅·전 도구 공통)

이 레지스터는 **여러 채팅과 Antigravity가 같이 쓰는 공유 상태**다. 아래를 지키지 않으면
정의 없는 라벨이 다시 떠돌기 시작한다 — 이 문서가 만들어진 바로 그 이유다.

### 규칙

1. **라벨을 인용하기 전에 이 문서를 먼저 읽는다.** `D6` 같은 ID를 근거로 쓰는 모든 문장은
   여기 정의가 있어야 한다. 없으면 그 문장은 무효다.
2. **새 미해결 항목은 여기 먼저 등록하고 ID를 받은 뒤** 다른 문서·채팅에서 인용한다.
   즉석에서 라벨을 만들지 않는다.
3. **항목에는 반드시 "막는 것"을 적는다.** 무엇이 이것 때문에 진행 불가인지 못 쓰면 항목이 아니다.
4. **해소 시 증거를 병기한다** — 실행 출력, 파일 경로, 조건(코너/온도/바이어스/W·L/추출법).
   "PASS"라는 보고는 증거가 아니다.
5. **폐기(`VOID`)도 기록한다.** A1·E2가 왜 사라졌는지가 다음 사람에게 필요하다.
6. **번호 재사용 금지.** 삭제 대신 `VOID` 처리.

### 역할별 책임

| 주체 | 이 문서에 대해 하는 일 | 하지 않는 일 |
| :--- | :--- | :--- |
| **사용자** | 시뮬·grep 실행, 결과를 채팅에 전달, 최종 정본 결정 | — |
| **LDO 설계 채팅** | 항목 등록·판정·상태 갱신 초안 작성 | 시뮬 실행 |
| **다른 설계 채팅**<br>(BGR / gm-Id LUT 등) | 자기 소관 항목(A군 provenance, LUT 사양)에 **증거로 회신**.<br>회신 시 **ID를 명시** | 다른 군의 항목을 임의 종결 |
| **Antigravity** | 파일 반영·git 커밋, 표 형식 정합성 유지 | **ID 신설 금지**, **상태 변경 금지**, 시뮬 실행 금지 |

### 커밋 규약

`OPEN_ITEMS.md`를 건드리는 커밋은 메시지 첫 줄에 영향받은 ID를 적는다.

```
docs(open-items): D6 CLOSED, D7 재정의 — pfet CGDO=0.194171 fF/um (tt)
```

---

## 1. 레지스터 (요약)

| ID | 항목 | 상태 | 결말 / 다음 행동 |
| :---: | :--- | :---: | :--- |
| A1 | pass `R_on ≈ 52 Ω` | **VOID** | 1차 출처 없음. C4가 대체 |
| A2 | BGR 트림 방향성 | CLOSED | 상향 확보 가능. 창 비대칭화 (고정부 38.9유닛) |
| A3 | 노이즈 `13~19 µV/√Hz` | CLOSED | devstdin **밴드갭** 값. 우리 소자 아님 |
| A4 | `Cc = 12.8 pF` | CLOSED | devstdin 복사값. Task 2의 **출력** |
| A5 | `C_out` 소자·밀도 | **CLOSED** | cap_mim_m3_1 단층, W=L=18.3147 µm 유닛 × 44 (m=44) = 30.000 pF |
| A6 | RO 전류 미확정 | **CLOSED** | $I_{RO} = 95.07\,\mu\text{A}$ ($f_{osc} = 110.89\,\text{MHz}$), $I_{max} = 2.13\,\text{mA}$ |
| A7 | 타일 폭 | **CLOSED** | `DIEAREA (0 0)(145360 225760)`, `UNITS 1000` → **145.36 × 225.76 = 32,816 µm²** |
| B1 | dropout 200/300 mV | CLOSED | A1 파생이라 무효 → C4로 재산출 |
| B2 | I_load,max | CLOSED | **4 mA.** 근거는 PDN 강하·gm 변동폭 |
| B3 | I_Q 범위 정의 | CLOSED | 시스템 기준 150 µA |
| B4 | I_Q 합산 | CLOSED | RO는 부하 → 60+EA+51 |
| B5 | LDO_EN | CLOSED | `uio[1]` |
| B6 | EA 입력 극성 | CLOSED | **PMOS 입력** (V_ICM 헤드룸 실측, D5) |
| B7 | LUT 격자 사양 | CLOSED | `lut/README.md` §3이 정본 |
| C1 | pass가 I_min에서 약반전인가 | **CLOSED** | gm/Id 15.05→19.06, **m≥40에서 포화** |
| C2 | LUT `CGD` 오버랩 포함? | **CLOSED** | **미포함.** 값은 D6 |
| C3 | `CGS` (HF PSRR 경로) | **CLOSED** | D6 보정 후에도 추종률 12.1%로 **거의 불변** |
| C4 | dropout(W) 곡선 | **CLOSED** | Step 1.5 산출 (`04` §1 반영) |
| C5 | LUT 자기검증 | **CLOSED** | 전 앵커 ≤0.003% |
| D1 | ff/125 °C `V_SG(I_min)` | OPEN | 코너 덱 |
| D2 | ff/125 °C 누설 | OPEN | 코너 덱 |
| D3 | ss dropout | OPEN | 코너 덱 |
| D4 | 넷리스트 geometry 규약 | PARTIAL | `.sch` 식 확인. **수치 대조 미실시** |
| D5 | `nfet` V_th @ss/−40 °C | CLOSED | 0.9055890 / 0.9655977 V |
| **D6** | 게이트 오버랩 `CGDO`/`CGSO` | **CLOSED** | **0.194171 fF/µm (tt).** §2 |
| **D7** | pass 접합캡 `Cdb`/`Csb` | OPEN | **grep 아닌 `.op` 측정으로 전환.** D4와 같은 덱 |
| **D8** | MiM `cpmimc` / 직렬 R (`r1`,`r2`) | OPEN | 둘레항과 ESR. C_out 설계 |
| **D10** | BGR 탭 브랜치 실측 | **OPEN** | 막는 것: Task 6 통합. 증거: 인계 문서 체크리스트 #1~5 |
| **D21** | thin-ox 실회로 앵커 | **CLOSED** | 부하싱크(`m=4`, geometry명시) 앵커 등록. $R_{on}=22.88\,\Omega$ |
| **D22** | BGR MC 평균 이동 | **OPEN** | $V_{ref}$ MC 평균 +0.83% 이동 (1.1965 V vs 1.1866 V). BGR 소관 |
| E1 | NMOS 입력 V_ICM | CLOSED | 마진 34 mV → PMOS 입력 |
| E2 | `nfet_01v8` LUT 부재 | **VOID** | E1이 PMOS로 귀결되어 소멸 |
| **E3** | 오버슈트 하드스펙 위반 | **CLOSED** | 슬루 지배. RC 드라이버 엣지 1.85 µs로 해소 |
| **E4** | met5 금지 → MiM 적층 불가 | **CLOSED** | `cap2m` 상판이 via4→met5. A5 재개 사유 |
| **E5** | W 상한의 실체 | **CLOSED(판정)** | C3/C4 아님. **I_Q ≤ 150 µA → m ≤ 60**. §2 |
| **E6** | 기동 중 pass 게이트 부유 | **CLOSED** | BGR startup 전코너 검증 완료로 fail-safe 미채택 확정 |
| **E8** | power-good 불채택 | **CLOSED** | `uo_out[0]` RO 출력이 대체 (`uo_out[1]` GND 타이오프) |
| **E9** | 트림 창 중심 재검토 | **OPEN** | MC-C ±3σ 외삽 상단 여유 0.31%로 얇음. 고정부 38.9→39.1유닛 조정 검토. BGR 소관 |

---

## 2. 주요 항목 상세

### D6 — 게이트 오버랩 **[CLOSED]**

**출처**: `libs.ref/sky130_fd_pr/spice/sky130_fd_pr__pfet_g5v0d10v5__tt.pm3.spice` L255-262

| 파라미터 | 값 (F/m) | 환산 | 비고 |
| :--- | ---: | ---: | :--- |
| `cgdo` = `cgso` | `1.94170991e-10` | **0.194171 fF/µm** | tt 코너 |
| `cf` | `1.2e-11` | 0.012 fF/µm | 외곽 프린지 |
| `cgbo` | `0.0` | — | 게이트-벌크 오버랩 없음 |

**코너 산포가 크다** (동일 파라미터, 각 `__<corner>.pm3.spice`):

| 코너 | fF/µm | tt 대비 |
| :--- | ---: | ---: |
| fs | 0.13840 | −28.7% |
| ff | 0.15249 | −21.5% |
| **tt** | **0.19417** | — |
| ss | 0.23688 | +22.0% |
| **sf** | **0.25544** | **+31.6%** |

→ **Task 4 코너 PM 검증에 `sf`를 포함할 것.** `cap_low`(camimc −11%)보다 이쪽이
$C_{gg}$를 더 크게 흔든다.

**LUT 보정식**: `C_gg,real = C_gg,LUT + 2 × 0.194171 × W_total[µm]` (fF).
동일 보정이 `C_gs`, `C_gd`에 각각 `1 ×` 로 적용된다.

**★ `C_gd`가 35배 커진다**: LUT 2.3 fF(W=400) → 실효 **80 fF**.
`04` §3.3의 "C_gd 2.5 fF로 pass 자체 Miller 무시 가능" 근거는 **폐기**.
단 결론(RHP zero 위협 낮음)은 유지 — $z = g_m/(C_c + C_{gd})$ = 52 MHz로 UGF의 10배.

### D7 — pass 접합캡 **[재정의]**

모델 카드 grep은 실패했다(`__model__*.model.spice`가 아니라 `.pm3.spice` 구조이고,
sky130 FET의 접합캡은 서브서킷 내부 다이오드일 수 있다).
**grep을 포기하고 `.op` 직접 측정으로 전환한다** — D4와 동일한 덱에서 같이 얻을 수 있다.
필요한 값: pass 드레인 노드의 총 캡(= `C_out`에 병렬로 들어감).
`ad_total` = 0.29 × W_total, `pd_total`은 분할 의존(§D4).

### D8 — MiM 둘레항과 ESR **[신규]**

`sky130_fd_pr__cap_mim_m3_1.model.spice` 실측 구조:
```
.param carea  = 'camimc*(wc)*(lc)'
.param cperim = 'cpmimc*((wc)+(lc))*2'
.param czero  = 'carea + cperim + MC_MM_SWITCH*AGAUSS(0,1,1)*0.01*2.8*(carea+cperim)/sqrt(wc*lc*mf)'
c1 c0 a 'czero' tc1=0 tc2=0.0
rs1 a  b1 'r1'  tc1={tc1rm3}   tc2={tc2rm3}
rs2 b1 c1 'r2'  tc1={tc1rvia3} tc2={tc2rvia3}
```
미확인: `cpmimc`(둘레 용량), `r1`/`r2`(met3·via3 직렬저항).
**ESR은 `C_out`에 LHP 영점을 만들므로 안정도에 유리할 수도 있다** — 정량화 필요.

### E3 — 부하 슬루 제약 **[CLOSED]**

슬루 드라이버(RC, R_slew 295.0k + C_slew 2.727p)로 구현 완료. 게이트 엣지
1.85us 양방향(코너산포 포함 1.61~2.09us, 하한 1.0us 통과). release/인가
양쪽 검증 통과(REPLY_LDO_TO_BGR_SLEWDRV 참조). 브랜치 3->1 통합에 따라
부하스펙 4mA->2.10mA 변경(§04_LDO_스펙 §2 반영).

### A5 — C_out 소자 선택 **[CLOSED]**

**결정**: cap_mim_m3_1 단층, W=L=18.3147 um 유닛 x 44 (m=44) = 30.000 pF.
      C_c는 동일 유닛 x 4 (m=4) = 2.7273 pF. 비 정확히 11.000.
      총 48유닛, 간격 0.84 um 포함 실효 면적 약 17,540 um^2.

**근거**: (a) DRC capm.6이 capm 최대 치수를 20 um로 제한 — 단일 플레이트 불가.
      (b) 동일 유닛 공유로 C_c/C_out 비가 공정·온도에 구조적으로 불변.
          p2/UGF = (g_m,pass/g_m,EA)(C_c/C_out)/beta 이므로 이 비가 곧 위상마진이다.
      (c) MF는 용량을 곱하지 않는다. m=N을 써야 한다 (MF=1과 MF=4 모두 678.9244 fF,
          MF=4 m=4가 2715.698 fF). MF는 미스매치 sigma를 sqrt(N)로 나누는 항에
          들어가므로 MC 시에는 m과 함께 적는다.
      (d) 에지 바이어스 0.040 um 보정 필요:
          C = 2.0(W-0.040)(L-0.040) + 0.19*2[(W-0.040)+(L-0.040)]  [fF, um]

**레이아웃 제약(Task 8 입력)**: capm.3 met3 0.14 um 둘러쌈, capm.2a 유닛 간 0.84 um,
      capm.11 무관 met3와 1.34 um 이격. 48유닛 배열 주변으로 met3 라우팅이 밀린다.

### A6 — RO 전류 미확정 **[CLOSED]**

링 단독 TB 실측으로 확정: I_RO = 95.07 uA (tt/27, 1.8V, 분주기 입력부하 5fF 포함).
f_osc = 110.89 MHz. I_avg = NCVf 관계가 5fF 추가 실험(+0.99 uA vs 예측 0.998 uA)으로
정확히 검증되어, 부하캡·단수와 무관하게 I_RO가 소자 크기로만 정해짐이 확인됨.
I_max = I_div(46) + I_RO(95) + I_snk(1993) = 2.13 mA.

### D11 — res_high_po_0p69 절대 공정 산포 sigma(R) **[OPEN, 부분 해소]**

**해소된 부분**: 모델의 결정론적 파라미터가 3점 피팅(잔차 0.00 ohm)으로 확정됐다.
    시트 470.976 ohm/um, 컨택 헤드 262.85 ohm/단자, 1유닛(L=4.416) 2605.5 ohm
    합성-L 공식: L_total = 5.5322*N - 1.1162 [um]
    BGR in-circuit 교차검증 -0.03%

**남은 부분**: MC 기반 sigma(R)는 여전히 미측정이다. R_z 절대값이 영점 위치를 정하므로
    산포가 크면 코너에서 p2 상쇄가 깨진다. 다만 Task 4 코너 스윕에서 R/C 코너
    기여가 PM 2.9 deg / GM 0.5 dB로 온도(5.7 deg / 2.9 dB)보다 작게 나와
    우선순위는 낮아졌다. MC 단계에서 확인한다.


### E5 — W 상한의 실체 **[판정]**

기존 인식(C3 EA 출력 천장 / C4 누설)은 **둘 다 여유가 크다**
(`V_EA@I_min` 2.31~2.46 V vs 천장 ≈3.0 V). 실제 사슬:

```
W ↑ → C_gg ↑ → Cc ↑ → g_m,EA = 2π·UGF·Cc ↑ → I_EA ↑ → I_Q ↑
```

D6 보정 후 (UGF 5.3 MHz, $C_{out}$ 30 pF, 입력쌍 gm/Id 16, 총 EA ≈ 4·I_D 가정):

| m | W [µm] | $C_{gg}$@I_min 실효 [fF] | $C_c$ [pF] | $I_{EA}$ [µA] | $I_Q$ [µA] | 판정 |
| ---: | ---: | ---: | ---: | ---: | ---: | :--- |
| 20 | 200 | 193.4 | 1.35 | 11.2 | 122 | ok |
| **40** | **400** | 373.9 | 2.72 | 22.7 | **134** | **ok** |
| 60 | 600 | 550.5 | 4.18 | 34.8 | 146 | 한계 |
| 80 | 800 | 724.7 | 5.76 | 48.0 | 159 | **초과** |

→ **$I_Q$ < 150 µA가 W 상한을 정한다. m ≤ 60.**

### D10 — BGR 탭 브랜치 실측 **[OPEN]**

* **내용**: BGR 코어 탭 브랜치 동작점 실측 및 전류 분배 정밀 검증
* **막는 것**: Task 6 부하 블록 시스템 통합
* **증거**: `00_인계_통합.md` 체크리스트 #1~5 항목

### D21 — thin-ox 실회로 앵커 **[CLOSED]**

LDO 부하싱크(nfet_01v8 W10 L0.15 m4, geometry명시, VGS=1.8 VDS=45.08mV
VSB=0, tt/27)로 등록 완료. 실측 ID=1.9706mA gds=0.05930S(intrinsic,
LUT대비+1.9%) Vth=0.72775V Cgg=40.887fF(triode). secant Ron=22.88옴은
LUT(1/gds)대비+16.7%이나, 이 차이는 직렬성분(nrd/nrs, ~3.3옴)과
secant/tangent 원리적 괴리(~2.7옴)로 분해됨(BGR REPLY_BGR_RON_GEO A/B대조).
결론: triode 영역 Ron은 LUT 조회가 아니라 geometry명시 .op로 확정할 것
(BGR lut/README.md 신설 규율과 동일).

추가 실회로 앵커: RO 링(nfet_01v8 W=0.42 / pfet_01v8 W=1, L=0.15, geometry 명시,
tt/27, VDDC 1.8V). LUT Isat 153.6 uA 대비 실효 구동전류 95.07 uA(0.61배)로,
포화 영역 조회값을 스위칭 회로에 그대로 쓰면 안 됨을 보인다. triode Ron 건과 함께
"LUT 조회값은 동작 영역을 확인하고 쓸 것"의 두 번째 사례.

### E6 — 기동 중 pass 게이트 부유 — fail-safe 클램프 불채택 **[CLOSED]**

BGR startup 전코너(staircase/slow-ramp, ss/-40 포함) 검증을 근거로
LDO 자체 fail-safe 미채택 확정. 3소자 NMOS 소스팔로워 안은 바디효과로
pass 게이트를 2.03V까지밖에 못 올려(그때 |Vsg|=1.27V > |Vth|=1.0V)
원리적으로 pass를 못 끔을 계산·시뮬로 확인. 5소자 안(PMOS풀업+인버터2단)은
가능하나 잔여리스크 대비 복잡도 과대. BGR 동의(REPLY_BGR_CBYP_CONFIRM,
REPLY_BGR_RON_GEO §6).

### E8 — power-good 불채택 **[CLOSED]**

RO 분주 출력(`uo_out[0]`)이 기능적 power-good을 대체한다. RO는 VDDC 급전이므로
LDO가 서지 않으면 발진하지 않고, 주파수가 출력 전압에 민감하도록 설계했으므로
(current-starved 불채택 근거) 1비트 PG보다 해상도가 높은 정보를 준다:
토글 없음=LDO 죽음 / 토글+예상주파수=정상 / 토글+주파수이탈=전압 이상.
별도 PG 비교기는 소자 7~9개, I_Q +2.56 uA, 문턱 정확도·히스테리시스·과도 오검출
검증 부담을 수반하나 온칩 소비자가 없다. `uo_out[1]`은 TT 규칙대로 GND 타이오프.

### D22 — BGR MC 평균 이동 **[OPEN]**

LDO 통합 MC(n=100, tt/27, MM+PR)에서 V_ref 평균이 1.19646 V로 골든(tt/27)
1.186591 V보다 +0.83% 높게 나왔다. 미스매치가 산포뿐 아니라 평균도 이동시킨
것으로 보인다(비선형 소자의 계통 오프셋 가능성). BGR 자체 MC에서도 동일 이동이
관측됐는지 확인 필요. 트림 창 중심 설계의 전제에 영향.

### E9 — 트림 창 중심 재검토 **[OPEN]**

MC-C 결과 트림 배수 필요 범위가 ±3σ 외삽에서 0.9490~1.0627, 가용 0.934~1.066
대비 상단 여유가 0.31%로 얇다. 분포 평균(V_out 1.7953 V)이 창 중심(1.8079 V)보다
12.6 mV 아래인 것이 원인. 고정부를 약 0.5% 키워 창을 위로 옮기는 것(예: 38.9 →
39.1유닛, 세그먼트 5.5유닛 유지)을 검토 요청. 단 유닛 수 정의가 문서 간 상이
(41유닛 vs L=244.537 → 44.4유닛)하므로 BGR 자체 기준으로 확인할 것.


