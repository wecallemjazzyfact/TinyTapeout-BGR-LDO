# 02_LUT_앵커.md (gm/Id Lookup Table 검증 앵커 및 특성 사양서)

최종 갱신: 2026-07 — LUT 생성·검증 완료 (전 게이트 PASS)

### TL;DR (3줄 요약)
1. SkyWater 130nm 4D gm/Id LUT의 신뢰성을 담보하는 확정 앵커 세트(A1/A1b/A2/A2b/A3)와 격자 사양을 규정합니다.
2. LUT는 2026-07 검증을 통과했으며, BGR 실회로 3소자 대조에서 ID ≤ +1.05% / GM ≤ +0.62% / GDS −3.2~+9.6% 정확도를 확인했습니다.
3. 미검증 수치 "0.647V" 계열의 폐기 근거를 물리 실측으로 재확인하고, W ≥ 5 µm 등 사용 규칙을 확정했습니다.

> **상세 정본**: 부호 규약, 생성 파이프라인, 정확도 분석, 사고 이력 전문은 **`lut/README.md`** 에 있습니다.
> 이 문서는 앵커와 사양의 요약본입니다.

---

## 1. 확정 검증 앵커 세트 (Anchors)

ngspice `.op` 직접 실측 (tt corner, 27°C, 단독 소자 구동, `nf=1`, geometry 파라미터 생략=PDK 기본값).
검증 덱: `bgr/anchor_n1.spice`, `anchor_n2.spice`, `anchor_p1.spice`, `lut/gen/a1b_remeasure.sp`.

| 앵커 ID | 소자 모델명 | W / L 치수 | 인가 바이어스 | 추출 매개변수 실측치 | 파생 성능지표 |
| :---: | :--- | :---: | :--- | :--- | :--- |
| **A1** | `nfet_g5v0d10v5` | $W=4.0\,\mu\text{m}$<br>$L=2.0\,\mu\text{m}$ | $V_{GS}=0.92$<br>$V_{DS}=1.00$<br>$V_{SB}=0.00$ | $V_{th}=0.80193\,\text{V}$<br>$I_D=2.1011\,\mu\text{A}$<br>$g_m=25.067\,\mu\text{S}$<br>$g_{ds}=48.953\,\text{nS}$<br>$V_{dsat}=0.12209\,\text{V}$ | $g_m/I_D=11.93\,\text{V}^{-1}$<br>$g_m/g_{ds}=512$ |
| **A1b** | `nfet_g5v0d10v5`<br>*(LUT 기준폭)* | $W=10.0\,\mu\text{m}$<br>$L=2.0\,\mu\text{m}$ | $V_{GS}=0.92$<br>$V_{DS}=1.00$<br>$V_{SB}=0.00$ | $V_{th}=0.7976068\,\text{V}$<br>$I_D=5.345498\,\mu\text{A}$<br>$g_m=62.91698\,\mu\text{S}$<br>$g_{ds}=71.20622\,\text{nS}$<br>$C_{gg}=41.79828\,\text{fF}$<br>$V_{dsat}=0.1206887\,\text{V}$ | $g_m/I_D=11.77\,\text{V}^{-1}$<br>**$g_m/g_{ds}=884$** |
| **A2** | `nfet_g5v0d10v5`<br>*(Body Effect)* | $W=4.0\,\mu\text{m}$<br>$L=2.0\,\mu\text{m}$ | $V_{GS}=0.92$<br>$V_{DS}=1.00$<br>$V_{SB}=0.78$ | $V_{th}=1.07310\,\text{V}$<br>$I_D=16.035\,\text{nA}$ | $\Delta V_{th}=+271\,\text{mV}$ |
| **A2b** | `nfet_g5v0d10v5`<br>*(Body Effect)* | $W=10.0\,\mu\text{m}$<br>$L=2.0\,\mu\text{m}$ | $V_{GS}=0.92$<br>$V_{DS}=1.00$<br>$V_{SB}=0.78$ | $V_{th}=1.068964\,\text{V}$<br>$I_D=41.13047\,\text{nA}$<br>$V_{dsat}=41.33217\,\text{mV}$ | **$\Delta V_{th}=+271.36\,\text{mV}$**<br>$V_{ov}=-149\,\text{mV}$ (약반전) |
| **A3** | `pfet_g5v0d10v5` | $W=10.0\,\mu\text{m}$<br>$L=2.0\,\mu\text{m}$ | $V_{SG}=1.176$<br>$V_{SD}=1.00$<br>$V_{SB}=0.00$ | $V_{th}=1.02434\,\text{V}$ (크기)<br>$I_D=3.0009\,\mu\text{A}$<br>$g_m=30.312\,\mu\text{S}$<br>$g_{ds}=172.18\,\text{nS}$<br>$V_{dsat}=0.15956\,\text{V}$ | $g_m/I_D=10.10\,\text{V}^{-1}$<br>$g_m/g_{ds}=176$ |

### 1.1. A1 vs A1b — 두 값 모두 옳다 (폭 명시 필수)

$g_m/g_{ds}$가 512(W=4) vs 884(W=10)로 크게 다르지만 **오류가 아닙니다.**
$W \approx 5\,\mu\text{m}$에 **모델 bin 경계**가 존재하여 $g_{ds}/W$가 W=4에서 +72% 이탈합니다.

동일 바이어스(nfet L=2, $V_{GS}$=0.92, $V_{DS}$=1.0, $V_{SB}$=0)에서 폭만 변경한 실측:

| W (µm) | 3 | 4 | **5** | 6 | 8 | 10 | 12 | 20 | 40 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| $g_{ds}/W$ (nS/µm) | 19.68 | 12.24 | **7.02** | 7.09 | 7.13 | 7.12 | 7.12 | 7.10 | 7.09 |
| $g_m/g_{ds}$ | — | 512 | — | — | — | 884 | — | 885 | 886 |

**$W \ge 5\,\mu\text{m}$에서 1.5% 이내 평탄.** 협폭 물리가 아니라 bin 경계인 근거: 같은 조건에서
$I_D/W$는 −1.7%, $g_m/W$는 −0.4%만 변하는데 $g_{ds}/W$만 +72% 튑니다. 매끄러운 협폭 효과라면
이동도·$V_{th}$를 흔들어 세 양이 함께 움직여야 하며, $g_{ds}$를 지배하는 CLM/DIBL 계열만
bin 간 불연속인 것이 sky130 모델의 전형입니다.

**intrinsic gain은 이론대로 L의 함수이고 W와 무관합니다** ($W\ge10$에서 884~886, 편차 0.2%).
**폭을 명시하지 않은 $g_{ds}$ · $g_m/g_{ds}$ 인용은 무효로 취급합니다.**

---

### 1.2. 코너 앵커 (LUT 격자 밖 — 헤드룸 판정용)

LUT는 **tt / 27 °C 전용**이므로 코너 수치는 테이블에 없습니다.
헤드룸 예산처럼 코너가 지배하는 판단에는 아래 실측을 사용합니다.
덱: `ldo/icm_chk.sp`. 조건: **ss 코너 / −40 °C / L = 2 µm / W = 10 µm / nf=1 / 기본 geometry**.

| 앵커 ID | 소자 | 조건 | `[vth]` 실측 | 용도 |
| :---: | :--- | :--- | ---: | :--- |
| **A4** | `nfet_g5v0d10v5` | $V_{SB} = 0$ | **`0.9055890 V`** | 온도·코너 축. tt/27 대비 **+108 mV** |
| **A5** | `nfet_g5v0d10v5` | $V_{SB} = 0.15\,\text{V}$ | **`0.9655977 V`** | $\Delta V_{th} = +60.0\,\text{mV}$ → $\gamma = 0.703\,\text{V}^{0.5}$ |
| **A6** | `pfet_g5v0d10v5` | $V_{SB} = 0$ | **`1.191607 V`** | EA 입력 극성 판정 근거 |

*   **$\gamma$ 역산**: $\Delta V_{th} = \gamma(\sqrt{2\phi_F + V_{SB}} - \sqrt{2\phi_F})$, $2\phi_F = 0.7$ 가정 →
    $\gamma = 0.703\,\text{V}^{0.5}$. A1/A2($V_{SB} = 0.78\,\text{V}$)에서 역산한 0.713과 정합합니다.
*   **설계 귀결**: $V_{ICM} = 1.2\,\text{V}$에서 NMOS 입력쌍 헤드룸 마진은 **34 mV**,
    PMOS 입력쌍은 **708 mV**입니다. 이 실측이 LDO 오차 증폭기를 **PMOS 입력**으로 확정한 근거입니다.
    상세는 `ldo/DECISIONS.md` D-001.
*   **주의**: 이 세 값은 **LUT 조회로 재현되지 않습니다.** 코너 수치를 LUT에서 읽으려 하지 마십시오.

---

## 2. 회로 레벨 교차 검증 (BGR 실회로 대조)

완성된 BGR의 `.op` 동작점을 LUT로 예측하여 실측과 대조했습니다.
**전 좌표가 격자 밖(off-grid)** 이므로 실사용 조건의 보간 품질을 측정합니다.
예측식: `LUT × (W/10) × m` (ngspice `@m[id]`는 이미 m 포함).

| 소자 | `XM3` | `XM3c` | `XM_top1` |
| :--- | :--- | :--- | :--- |
| 소자형 / W / L / m | nfet 20/4/2 | nfet 10/2/2 | pfet 20/4/4 |
| $V_{GS}$ | 1.190078 | 1.299742 | 1.176203 |
| $V_{DS}$ | 0.4381737 | 0.7517368 | 0.3011255 |
| $V_{SB}$ | 0.778899 | **1.21717** (격자 밖) | 5.60124e−5 |
| $I_D$ 실측 | 10.25296 µA | 10.25296 µA | 10.25294 µA |
| $g_m$ 실측 | 130.4483 µS | 132.7897 µS | 112.6772 µS |
| $g_{ds}$ 실측 | 597.7988 nS | 217.4945 nS | 962.2210 nS |
| $C_{gg}$ 실측 | 335.5136 fF | 80.26672 fF | 512.7140 fF |
| **$I_D$ 예측 오차** | **−0.04%** | **+0.94%** | **+1.05%** |
| **$g_m$ 예측 오차** | +0.21% | −0.03% | +0.62% |
| **$C_{gg}$ 예측 오차** | +0.57% | +0.01% | +0.12% |
| **$g_{ds}$ 예측 오차** | +9.57% | −3.21% | +2.31% |

**해석**
*   `XM_top1`은 $V_{DS}=0.301\,\text{V}$가 $v_{dsat}=0.158\,\text{V}$ 바로 위(포화 무릎)로,
    $V_{DS}$ 100 mV 격자의 실효성을 시험합니다 → $g_{ds}$ +2.31%. 구 7점 격자였다면 수십 % 오차가 났을 지점입니다.
*   `XM3c`는 $V_{SB}$가 격자 상한 1.2 V를 17 mV 초과한 외삽인데도 $I_D$ +0.94% → **소폭 외삽은 안전**합니다.
*   $g_{ds}$만 한 자릿수 나쁩니다(최악 +9.57%). 설계 시 이를 전제로 삼습니다(§5 규칙 참조).

---

## 3. 폐기 이력 및 오염 규정 명세

### 3.1. NFET 0.647V 문턱 전압 앵커 폐기 — **인용 금지**

*   **대상**: `nfet_g5v0d10v5 Vth(VSB=0, tt) ≈ 0.647V`, `(ss) ≈ 0.716V`, `(VSB=0.78) ≈ 1.068V`
*   **최초 폐기 근거**: 근거 추적 결과 ngspice BSIM 모델 카드 직접 추출이나 넷리스트 DC 측정 데이터가 아니었으며,
    초기 BGR 설계 전개 중 검증 없이 유입된 수치적 가정이 오용된 것으로 규명됨.
*   **2026-07 독립 재확인** — 물리 실측으로 폐기 판정이 재확인되었습니다:
    1.  **repo 전체 grep 결과 1차 출처가 존재하지 않음.** 시뮬 로그·CSV·설계 문서 어디에도 없고,
        인계문서를 받아 적은 AI 파생 파일에만 존재했습니다.
    2.  전 L 격자의 `[vth]`(tt/27/VSB=0)는 **0.79 ~ 0.84 V** 범위로 0.647과 불일치.
    3.  **자체 모순**: `1.068 − 0.800 = +268 mV`로 문서의 바디이펙트 "+270 mV"와 정합 →
        1.068은 **0.800 계열**이지 0.647 계열이 아닙니다.
    4.  **온도설 기각**: 125 °C `[vth]` 실측은 **0.674 V**로 0.647과 다릅니다.
*   **대체 앵커**: A1(W=4, 0.80193 V) / A1b(W=10, 0.7976068 V). 바디이펙트는 A2/A2b의 $\Delta V_{th}$.
*   **이 사건이 "조건·출처 없는 수치는 앵커 자격이 없다"는 프로젝트 규칙의 존재 이유입니다.**

### 3.2. 구 M_pd 스타트업 미니스윕 절대 전류 6배 오염

*   **내용**: 스타트업 차단 능력 검토 당시 0.777V 인가 시 $0.208\,\mu\text{A}$, 0.92V 인가 시 $12.97\,\mu\text{A}$
    미니스윕 절대 전류가 실제 물리 특성 대비 약 6배 높게 정착되어 있었음을 확인.
*   **원인**: 두 측정점의 절대 스케일이 동일 배율로 튄 정황으로 보아 **계통 오차**.
    테스트벤치 결선 시 $M$ 또는 $W$ 치수 오용, 혹은 B-Source 전류 경로 상 기생 경로 합성으로 분석됨.
*   **설계 영향 무해성**:
    1.  두 바이어스 점 간 비율(62배)은 정확히 정합하여 당시 subthreshold 경계 기동 차단 논증은 손상되지 않음.
    2.  `M_pd` 최종 W 확장(4 ➜ 12) 결정은 이 미니스윕이 아닌 **ss/-40°C 기동 과도 시뮬레이션 실패 분석**과
        **잔류 기동 전류 실측치(33 fA / 0.026 fA)** 기반이므로 최종 회로 결론은 유효.

---

## 4. LUT 생성 사양 및 검증 합격 기준

*   **PDK 소자 대상**: `sky130_fd_pr__nfet_g5v0d10v5`, `sky130_fd_pr__pfet_g5v0d10v5`
*   **기준 폭**: $W = 10\,\mu\text{m}$, `nf=1`, `m=1`
*   **코너 / 온도**: `tt` / 27 °C **단일** (코너 영향은 회로 시뮬에서 검증 — Murmann 표준 플로우)
*   **소자 geometry**: `ad/as/pd/ps/nrd/nrs/sa/sb/sd` **생략 (PDK 기본값)**
    → 검증 앵커 덱과 정합 필수. 기생 강제 제거(`nrd=0` 등) 시 동일 바이어스에서 $I_D$가 **2.9% 어긋나**
    ±2% 기준을 통과할 수 없습니다.

### 4.1. 스윕 축 정의 (Grid Axes) — 최종 형상 `(L, VGS, VDS, VSB) = (6, 135, 34, 5)`

| 축 | 점수 | 값 |
| :--- | :---: | :--- |
| **$L$** | 6 | `[0.5, 1.0, 2.0, 4.0, 8.0, 20.0]` µm (연속 bsimg4 bin 영역) |
| **$V_{GS}$** | 135 | `0.0 ~ 3.3 V` 25 mV 균일 133점 **+ 앵커점 `0.920`, `1.176` 삽입** |
| **$V_{DS}$** | 34 | `0.0 ~ 3.3 V` **100 mV 균일** |
| **$V_{SB}$** | 5 | `[0.0, 0.4, 0.78, 0.8, 1.2]` V (`0.78`은 A2 앵커점) |

> **구 7점 $V_{DS}$ 안 `[0.1, 0.3, 0.6, 1.0, 1.65, 2.5, 3.3]`은 폐기되었습니다.**
> 포화 무릎($V_{DS} \approx V_{ov}$) 부근에서 $g_{ds}$가 급격히 휘므로 성긴 격자 + 선형보간이면
> 조회 오차가 수십 %에 달하며, $g_{ds}$는 EA 전압 이득 계산의 분모입니다.

*   **추출/저장 매개변수 (9종)**: `ID`, `VT`, `GM`, `GDS`, `CGG`, `CGS`, `CGD`, `CGB`, `VDSAT`
*   **파생 가능**: $g_m/I_D$, $I_D/W$, $g_m/g_{ds}$, $f_T = g_m/(2\pi C_{gg})$, $V_{ov}$

### 4.2. 검증 합격 기준 (Pass Criteria)

1.  **격자 정점 앵커** A1b / A2b / A3 조회가 실측 대비 **±2% 이내** (전압은 ±2% 또는 ±10 mV)
2.  **실회로 앵커** (BGR `XM3` / `XM3c` / `XM_top1`) $I_D$·$g_m$ **±5%**, $C_{gg}$ **±10%**
3.  **파이프라인 무결성 게이트**: 전하보존 $|C_{gg}-(C_{gs}+C_{gd}+C_{gb})|/C_{gg} < 3\%$,
    $I_D > 0$, $I_D$의 $V_{GS}$ 단조 증가, $V_{th}$의 $V_{SB}$ 단조 증가

**2026-07 실측 달성치**: 격자 앵커 ≤ **0.0043%**, 실회로 $I_D$ ≤ +1.05% / $g_m$ ≤ +0.62% / $C_{gg}$ ≤ +0.57%,
무결성 게이트 8개 전부 PASS (전하보존 0.000%, 단조성 위반 0.00%).
검증 스크립트: `lut/gen/verify_gmid.py` (순수 python, ngspice 불요).

---

## 5. LUT 사용 규칙 (필수 준수)

조회는 `lut/lookup.py` 단일 경로. **상세는 `lut/README.md`가 단일 정본.**

### 5.1. 정확도 전제

| 변수 | 격자 정점 | off-grid 실회로 | 설계 시 전제 |
| :--- | :---: | :---: | :--- |
| `ID` | 0.00% | −0.04 ~ **+1.05%** | 신뢰. 사이징 1차 근거로 사용 |
| `GM` | 0.00% | −0.03 ~ +0.62% | 신뢰. UGF / $g_m$ 예산에 사용 |
| `CGG` | 0.00% | +0.01 ~ +0.57% | 신뢰. 보상망 설계에 사용 |
| **`GDS`** | 0.00% | **−3.2 ~ +9.6%** | **±10~20% 전제. 이득 확정은 회로 시뮬로** |
| `VT`, `VDSAT` | 0.00% | — | 격자 정점 기준 |

### 5.2. 사용 규칙

1.  **유닛 핑거 $W \ge 5\,\mu\text{m}$ (권장 $10\,\mu\text{m}$)**, 면적은 `m`으로 확보.
    W=4에서 $g_{ds}/W$가 +72% 이탈합니다(§1.1).
    또한 $W$=10 ↔ 400 사이에 잔여 폭 의존성 ~2%가 있으므로 **대형 소자는 `m` × (W=10 핑거)** 로 구성합니다.
    (예: LDO pass device W=400 → `m=40, W=10`)
2.  **$L$은 격자값만** `{0.5, 1, 2, 4, 8, 20}` µm. RSCE로 $L$에 비단조(L=2 극소 후 L=4 반등)라
    보간 금지 — `lookup.py`가 `ValueError`로 강제합니다.
3.  **스케일** `= lookup × (W/10) × m`. ngspice `@m.x<inst>.<model>[id]`는 **이미 m을 포함**합니다.
4.  **`[vth]`는 $V_{DS}$ 무의존 zero-bias 계열**이며 동작점 실효 문턱이 아닙니다
    ($V_{DS}$ 0.1↔1.65 V에서 6 ppm 변동). $V_{SB}$·온도 의존성은 정상이므로 바디이펙트 sanity용으로만 쓰고,
    **정밀 사이징은 $g_m/I_D$ 와 $v_{dsat}$** 으로 합니다.
5.  **$V_{SB}$ 격자 상한 1.2 V.** 소폭 외삽은 검증됨(1.217 V에서 $I_D$ +0.94%).
    크게 넘는 소자가 나오면 격자 확장을 검토합니다.
6.  테이블은 **tt 전용**. 코너 영향은 회로 시뮬에서 검증합니다.
7.  LUT 사이징 결과는 **제안**이며 확정은 회로 시뮬 검증 후입니다.

### 5.3. 소자 파라미터 참고값 (nfet, tt/27, VSB=0, W=10)

$V_{th}$의 $L$ 의존성 — **비단조 (Reverse Short Channel Effect)**:

| L (µm) | 0.5 | 1 | 2 | 4 | 8 | 20 |
| :--- | ---: | ---: | ---: | ---: | ---: | ---: |
| $V_{th}$ (V) | 0.8361 | 0.8151 | 0.7976 | 0.8001 | 0.7932 | 0.7925 |

---

## 6. pygmid 데이터 포맷 및 부호 규약

### 6.1. 딕셔너리 구조

```python
{
    # 축 정의 (1D Numpy array)
    'L':   np.array([5e-7, 1e-6, 2e-6, 4e-6, 8e-6, 2e-5]),          # 6
    'VSB': np.array([0.0, 0.4, 0.78, 0.8, 1.2]),                     # 5
    'VDS': np.array([0.0, 0.1, ..., 3.3]),                           # 34
    'VGS': np.array([0.0, 0.025, ..., 0.920, ..., 1.176, ..., 3.3]), # 135

    # 4D 데이터 테이블 — 차원 순서 (L, VGS, VDS, VSB) 고정
    'ID': np.zeros((6, 135, 34, 5)),
    'VT': ..., 'GM': ..., 'GDS': ...,
    'CGG': ..., 'CGS': ..., 'CGD': ..., 'CGB': ..., 'VDSAT': ...,
}
```

*   **주의**: interpn 조회 시 차원축 순서 `(L, VGS, VDS, VSB)`를 임의 왜곡하면 보간 좌표가 파괴됩니다.
    다차원 전치나 인덱싱 결합 시 축 순서를 고정 유지하십시오.

### 6.2. 부호 규약 (요약 — 상세는 `lut/README.md` §4)

*   `ID`, `VT`, `VDSAT`, `GM`, `GDS`, `CGG` → **양수 크기**, nfet/pfet 동일
*   `CGS`, `CGD`, `CGB` → **signed 유지**. `CGD`는 포화에서 물리적으로 0을 교차하므로 저장 시 `abs()` 금지이며,
    `CGG = CGS + CGD + CGB` 전하보존이 파싱·저장 경로의 체크섬으로 작동합니다.
    조회 시에만 `abs()`가 적용됩니다.
*   정상 테이블에도 $V_{DS}=0$ 열에 수치 먼지(~1e−22 A)가 남습니다.
    부호 위반 판정은 `x < −1e−9 × max|x|` 기준입니다.

### 6.3. 생성 방식

pygmid의 `sweep` 모듈은 Cadence Spectre 전용(`pysweep.scs` / `SpectreSimulator` / `psf_utils`)이라
ngspice에서 구동 불가합니다. 따라서 **자체 ngspice 스윕(`lut/gen/run_sweep.py`) + pygmid 포맷 저장**으로
생성하고, 조회부인 `pygmid.Lookup`(시뮬레이터 독립)을 그대로 사용합니다.

재생성 소요: nfet 14분 23초, pfet 11분 45초. **ngspice를 호출하므로 `bash -l -c` 필수**입니다.

---

## 7. chennakeshavadasa 공개 레포지토리 데이터 지위

*   **정합 확인**: 해당 공개 레포의 $V_{th}(L=2) = 0.774\,\text{V}$는 $V_{DS}$가 높은 상황의 DIBL이 반영된 수치로,
    당사 실측($0.7976\,\text{V}$ @$V_{DS}=1.0$, W=10)과 물리적 크기 및 변화 거동이 정합합니다.
*   **활용 원칙**
    1.  **형상 곡선 신뢰**: W로 정규화되어 무차원화된 형상 개형($g_m/I_D$ vs $V_{ov}$, $g_m/g_{ds}$ 곡선)은
        신뢰 가능한 설계 참조물입니다.
    2.  **자체 LUT 우선**: 해당 레포의 5V Thick-Ox FET 스윕 덱 조건(특히 $V_{DS}$ 축 바이어스)이
        공개되지 않았으므로, 설계의 절대 스케일링($I_D/W$, $g_m$ 절대량) 매핑은 **자체 LUT를 최우선**으로 삼고
        공개 레포는 교차 대조용 2차 자산으로 한정합니다.
