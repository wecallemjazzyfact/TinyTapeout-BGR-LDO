# 00_인계_통합.md (프로젝트 마스터 인계 문서)
최종 갱신: 2026-08-19 — BGR+LDO 통합 레이아웃 완료, PEX R+C 및 Final Monte Carlo (N=299, 3σ=15.22mV) 검증 완료, TinyTapeout 브랜치 머지 완료

### TL;DR (3줄 요약)
1. BGR(v6) + LDO 통합 칩 레이아웃(DRC 0, LVS Match, Antenna 해소) 및 TinyTapeout GitHub 브랜치 머지를 완결했습니다.
2. PEX R+C 기반 Final Monte Carlo(N=299) 검증 결과, **트림 후 VDDC $\sigma = 5.073\,\text{mV}$, $3\sigma = 15.22\,\text{mV}$**로 하드 스펙(±36mV) 대비 2.4배 여유를 사수했습니다.
3. 상세 검증 수치 및 대회 문서화 가이드는 **`05_최종_레이아웃_검증_요약.md`** 및 `04_LDO_스펙.md`가 정본입니다.

> **문서 위계**: 판정의 근거와 대체 이력은 `designs/bgr_ldo/ldo/DECISIONS.md`가 정본입니다.
> 이 문서와 충돌하면 DECISIONS.md를 따르십시오. LDO 상세 사양은 `04_LDO_스펙.md`, 레이아웃/MC 검증은 `05_최종_레이아웃_검증_요약.md`.

---

## 1. 프로젝트 개요

*   **프로젝트명**: SkyWater 130nm BGR + LDO 통합 아날로그 IP 설계 및 TinyTapeout Analog Shuttle 제출
*   **Target PDK**: SkyWater Sky130A (`tt` Typical 코너 기준, 코너 검증은 회로 시뮬로)
*   **플랫폼**: TinyTapeout Analog Shuttle, 1x2 타일 **3.3 V `VAPWR` 템플릿** (`tt_analog_1x2_3v3.def`)
*   **제출 마감**: 2026년 9월 7일 (TTSKY26c)
*   **개발 단계**: **BGR+LDO 레이아웃 및 PEX/MC 종합 검증 완료 ➜ GitHub 브랜치 머지 완료 (테이프아웃 완료)**

---

## 2. 프로젝트 폴더 구조

```
c:\Users\aa\Desktop\school\TinyTapeout\        (도커: /foss/designs/)
├─ 00_인계_통합.md          # 이 문서 — 마스터 인계
├─ 01_BGR_최종스펙.md       # BGR 실측 스펙 및 설계 여정
├─ 02_LUT_앵커.md           # gm/Id LUT 검증 앵커 및 사용 규칙
├─ 03_PDK_환경.md           # PDK 소자·환경·실행 규칙
├─ 04_LDO_스펙.md           # LDO 목표 스펙 및 아키텍처
├─ 05_최종_레이아웃_검증_요약.md # ★ 레이아웃/PEX/Final MC 최종 검증서 [NEW]
├─ WORKSPACE.md / RESTART_GUIDE.md / tapeout-guardrails.md
├─ sky130A_corner_structure.md # Sky130A 코너 구조 정본
├─ designs/
│  └─ bgr_ldo/               # git repo 루트
│     ├─ DESIGN_JOURNEY.md   # 설계 진화 이력 (v1~v6 BGR, v7~ LDO)
│     ├─ LOGBOOK.md          # 시간순 설계 일지
│     ├─ NOTES.md            # 하드 제약 및 고정 사양
│     ├─ RECIPES.md          # 특성화 덱 및 ngspice 실행 템플릿
│     ├─ REFERENCE_NOTES.md  # 레퍼런스 프로젝트 분석
│     ├─ bgr/                # BGR 정본 (동결). bgr_core_tb.spice, anchor_*.spice, mc_loop.py
│     ├─ ldo/                # LDO 작업 폴더
│     │  └─ DECISIONS.md     # ★ LDO 판정 기록 (근거·대체 이력의 정본)
│     ├─ top/                # BGR + LDO 통합 및 핀 매핑
│     ├─ layout/             # Magic 레이아웃 및 GDS/LEF
│     ├─ milestones/         # 01~09 = BGR, 10~ = LDO
│     ├─ work/               # DRC/LVS 임시
│     └─ lut/                # gm/Id LUT (프로젝트 공용)
│        ├─ README.md        # ★ LUT 정본 문서 (사양·부호규약·정확도·사고이력)
│        ├─ lookup.py        # 조회 API
│        ├─ design.py        # 사이징 계층 (curves/vgs_at/size/charts)
│        ├─ pygmid/          # Lookup 모듈만 사용
│        ├─ gen/             # run_sweep.py, verify_gmid.py, 앵커 덱
│        └─ data/            # nfet/pfet_g5v0d10v5.pkl
```

**경로 규율**: 구 `sim/` 참조는 전부 `bgr/`로 읽을 것.
`ldo/xschemrc`의 `netlist_dir`이 `.../ldo`로 잡혔는지 확인 — 경로 이원화가 stale-file 사고의 근본 원인이었다.

---

## 3. 작업 규칙 (사고 이력 기반 — 엄수)

1. **시뮬레이션과 스키매틱 수정은 사용자가 직접 수행.** Claude는 설계 판단·계산·해석·명령어 작성·문서만.
   Antigravity는 스크립트·문서·배치·커밋만, **시뮬 실행 금지**.
2. **파일 작업은 `bash -c`, 툴 실행은 반드시 `bash -l -c`.**
   `bash -c`는 PATH를 로드하지 않는다. **ngspice를 호출하는 python 스크립트도 `bash -l -c`** (subprocess가 PATH 상속).
3. **정본 → `cp run_*.spice` 사본 → 사본으로 시뮬.** 정본 `.control` 직접 수정 금지.
4. **시뮬 전 `grep -A1`로 소자값 대조** (continuation `+` 줄에 `m`/`mult`가 숨는다).
5. ngspice `.control`에서 **`;`는 주석** — 한 줄에 명령 두 개 금지. docker 명령은 한 번에 하나.
6. **수치 규율(최상위)**: 소자 수치는 `lut/lookup.py` 조회 또는 당세션 ngspice 실측만 인용.
   기억·추정 금지. 모든 수치에 조건(코너/온도/바이어스/W·L·geometry/추출법) 병기.
   **조건 없는 수치는 무효.**
7. AI 제안 사이징·수치는 전부 '제안'이며 **확정은 회로 시뮬 검증 후.**
8. **검증은 직접 실행 출력 + 원시 데이터로만.** 보고서의 "PASS"는 증거가 아니다.
   재스윕·재생성 후에는 산출물의 mtime을 확인할 것.

---

## 4. BGR 완성 상태 (동결)

v6 BGR 코어 설계 종결. 상세는 `01_BGR_최종스펙.md`.

*   **동작점**: 3.3 V `VAPWR`에서 $V_{ref}(\text{VREF\_LOW}) = 1.18659\,\text{V}$ (27 °C, tt, 트림 중앙)
*   **온도 계수**: tt `7.5` / ss `8.6` / ff `9.0` ppm/°C (−40 ~ 125 °C)
*   **라인 레귤레이션**: `0.0840 %/V` (NMOS Cascode 등화 차폐 적용)
*   **미스매치**: MC 100회 $\sigma = 2.21\%$ (±3σ = ±6.6%)
*   **정지전류**: 59.73 µA
*   **출력 임피던스**: 121 kΩ (R2 = 41유닛). LDO EA 게이트 입력으로는 무해하나 **핀 측정 시 주의**
    (10 MΩ DMM이면 1.2% 분압 오차 → **SMU/전위계 >100 MΩ 필요**)
*   **디지털 트림**: 5-bit, 제어 `ui_in[4:0]`. **사양 2건 수정됨 — `01` §6 및 DECISIONS D-004/D-005 참조**
    *   트림 창 **비대칭화**: 고정부 38.9유닛, 세그먼트 총합 5.5유닛 (목표가 1.2 V이므로 대칭 창은 −3σ에서 1.3% 부족)
    *   스위치 **R_on ≤ 5 Ω** (총 ≤25 Ω). 50 Ω이면 R_on의 TC가 BGR TC를 두 배로 만든다

---

## 5. LDO 사양 요약

**정본은 `04_LDO_스펙.md`.** 여기서는 탑레벨 결정만 요약한다.

### 5.1. 전력 및 동작 범위
| 항목 | 값 |
| :--- | :--- |
| 입력 전원 | 3.3 V (`VAPWR`) |
| 출력 전압 | **1.8 V ±2%** (전 코너·온도, 트림 후) |
| 부하 전류 | **50.8 µA ~ 2.22 mA** ($I_{max} = 2218.6\,\mu\text{A}$, 최소는 피드백 분압기가 보장) |
| RO | **5단** (NAND 1 + 인버터 4), 부하캡 5개 W=L=4.27, VDDC 급전 |
| $f_{osc}$ | **243.97 MHz** (tt/27, VDDC 1.79075) |
| DIV_OUT | **15.248 MHz** (÷16) |
| $I_{RO}$ | **96.60 µA** |
| $I_{max}$ | **2218.6 µA** |
| 정지전류 | **140.04 µA** (기존 139.10 + startup 클램프 inv1 관통 0.94 µA) |
| 드롭아웃 | **미확정.** 구 200/300 mV 값은 근거 없어 폐기 (DECISIONS D-002). LUT triode 조회로 재산출 |

### 5.2. 회로 토폴로지
*   **패스 소자**: 대형 PMOS `sky130_fd_pr__pfet_g5v0d10v5`.
    실측 앵커 W=400/L=0.5 기준이나 **상한 근처이므로 아래로 탐색** (C_gg ≤ 0.15 C_c).
    레이아웃은 **m × (W=10 µm 핑거)** — LUT 정확도와 정석 양쪽 이유.
*   **오차 증폭기**: **PMOS 입력 Folded-Cascode**, `VAPWR` 직결.
    **근거 정정**: 과거의 "출력 상단 스윙" 논리는 틀렸다 — folded cascode 출력 스윙은
    출력단 캐스코드 미러가 정하며 입력 극성과 무관하다.
    실제 근거는 **V_ICM 헤드룸**이며 ss/−40 °C 실측에서 NMOS 마진 34 mV vs PMOS 708 mV.
    (DECISIONS D-001)
*   **주파수 보상**: 표준 Miller + $R_z$ 1순위. Ahuja는 PSRR 개선 옵션.
    **$C_c$는 Task 2의 출력이지 입력이 아니다.** 12.8 pF는 레퍼런스 복사값이며 우리 설계값이 아님.
*   **피드백 분압**: $R_a = 4$유닛(11.8 k), $R_b = 8$유닛(23.6 k), 비 1.5. BGR 유닛저항 재사용.

### 5.3. 온칩 부하 블록
아날로그 핀으로 부하를 끌면 TT 경로 저항(레퍼런스 실측 160 Ω)이 측정을 지배하므로,
**부하는 전부 온칩에 두고 `ua`는 전압 sense 전용**으로 쓴다.
*   **RO 데모 부하**: 일반 인버터 링(01v8) 5단 + 분주기 ÷16. current-starved 불채택 —
    그 구조는 공급 전압에 둔감하나 **우리는 f를 LDO 품질의 프록시로 쓰므로 민감도가 기능**이다.
    **확정 실측: $f_{osc}$ 243.97 MHz, $I_{RO}$ 96.60 µA, DIV_OUT 15.248 MHz.**
    링은 VDDC 급전, 분주기는 VDPWR 급전(도메인 분리).
*   **특성 부하**: 단일 2 mA 싱크 + 슬루 드라이버(RC) 1벌. 버퍼는 VDDC 급전.

### 5.4. 핀 배정

| 핀 | 신호 | | 핀 | 신호 |
| :--- | :--- |---| :--- | :--- |
| `ui_in[3:0]` | TRIM[3:0] | | `uo_out[0]` | **DIV_OUT** |
| `ui_in[4]` | **SNK_EN** | | `uo_out[7:1]` | 미사용 (project.v 가 0) |
| `ui_in[5]` | RO_EN | | `ua[0]` | VREF_LOW |
| `ui_in[7:6]` | 미사용 | | `ua[1]` | **VDDC (트림 기준)** |
| `uio[7:0]` | 미사용 | | `ua[7:2]` | 미연결 (D26 참조) |

**변경 사유**: `ui_in[0]`~`ui_in[5]`가 물리적으로 인접해 배선이 짧아진다. 기존
`ui_in[7]` 배정은 임의였고 전기적 이유가 없었다.

### 5.4.1 전원 인가 시 입력 권장 상태

| 핀 | 권장 | 이유 |
| :--- | :---: | :--- |
| `ui_in[3:0]` (TRIM) | **0000** | top 인버터 반전으로 V_out 최소(가장 안전). TT 가 비활성 시 0 으로 묶으므로 기본값이 이미 안전 |
| `ui_in[4]` (SNK_EN) | **0** | 부하 에뮬레이터 OFF |
| `ui_in[5]` (RO_EN) | **0** | 링 발진기 OFF |

RO_EN/SNK_EN 이 1 인 채로 전원이 인가돼도 회로는 안전하다 — 게이트 산화막 전압이
정상 동작 시(pfet V_sg = 1.79 V)와 같은 1.8 V 이고, 싱크 버퍼가 VDDC 급전이라
VDDC=0 이면 vg_snk 가 C_slew 로 0 에 묶여 싱크가 꺼져 있다(자기 순서화).
권장일 뿐 필수는 아니다.

**미구동 `uo_out`/`uio_out`/`uio_oe`는 GND 연결 필수.**

### 5.5. 면적 예산
1x2 3v3 타일 기준. **폭이 145.36 vs 160 µm로 미확정 — `tt_analog_1x2_3v3.def`의 DIEAREA로 확정할 것**
(9% 차이라 유의미).

| 블록 | 예산 |
| :--- | ---: |
| BGR (완성) | ~5,500 µm² |
| 5-bit 트림 | ~1,200 µm² (레벨시프터 제거로 축소 여지) |
| **LDO** | **~14,000 µm²** — C_out과 C_c가 최대 변수 |
| RO + 분주기 + 싱크 | ~1,100 µm² |
| 라우팅 | ~2,500 µm² |

**C_out은 MiM 적층(4.0 fF/µm²)을 액티브 위에 배치**하므로 실면적 벌금이 거의 없다 (DECISIONS D-003).

---

## 6. Task 진행 현황

| Task | 내용 | 상태 |
| :---: | :--- | :--- |
| **0** | gm/Id LUT 인프라 구축 및 검증 | **완료** — 격자 앵커 ≤0.0043%, 실회로 ID ≤+1.05% |
| **0.3** | BGR/LDO 폴더 재구성 및 경로 패치 | **완료** |
| **0.5** | TT 스펙 조사, 도메인 아키텍처, 부하 범위 확정 | **완료** |
| **1** | **Pass PMOS 사이징 (LUT 기반) → C_gg 확정 → 극점 지형** | **진행 중** |
| 2 | 보상 전략 (`C_out × UGF` 최대화) → EA 전류 예산 | 대기 |
| 3 | EA (PMOS 입력 folded cascode) LUT 사이징 → 회로 검증 | 대기 |
| 4 | 보상망 → 안정도 (전 부하 × 전 코너 PM > 45°) | 대기 |
| 5 | load/line transient, 오버슈트 < 1.95 V, 슬루 드라이버(완료), fail-safe(불채택, E6 CLOSED), power-good(**불채택, E8 CLOSED**) | **완료** |
| 6 | 부하 블록 + BGR 통합 + 트림 end-to-end + **startup 클램프** → top 통합 | **완료 (PEX 전)** |
| 7 | 5-bit 트림 회로 구현 | 대기 |
| 8 | 레이아웃 → DRC/LVS → PEX | 대기 |
| 9 | 전체 특성 매트릭스 (코너 + MC + PEX 재검증) → 제출 | **MC 선행분 완료(A/B/C), PEX 후 재실행 대기** |

**측정 방침**: 설계 중 시뮬은 설계 검증용만(PM, transient, op).
**전체 특성 매트릭스와 MC는 PEX 후 Task 9에서 한 번에.** BGR의 PSRR/노이즈 미측정분도 그때 함께
(LDO 노이즈 스펙의 전제조건).
