# 04_LDO_스펙.md (LDO 목표 사양 및 아키텍처 확정서)

최종 갱신: 2026-07 — EA 극성 재판정, 과도 한계 반영

### TL;DR (3줄 요약)
1. VAPWR 3.3 V → 1.8 V, 부하 50.8 µA ~ 4 mA의 point-of-load LDO 목표 사양과 그 도출 근거를 규정합니다.
2. **PMOS 입력 Folded-Cascode EA + PMOS pass** 구조이며, 극성 근거는 V_ICM 헤드룸 실측입니다(과거 "출력 스윙" 논리는 폐기).
3. capless + 소면적의 과도응답 물리 한계를 계산하여 스펙을 3단 계층화하고, Task 2의 목적함수를 `C_out × UGF` 최대화로 확정했습니다.

> **판정 근거와 대체 이력은 `designs/bgr_ldo/ldo/DECISIONS.md`가 정본**입니다.
> 이 문서는 결론의 요약이며, 충돌 시 DECISIONS.md를 따르십시오.

---

## 1. 목표 스펙

| 항목 | 조건 | 목표 | 근거 |
| :--- | :--- | :--- | :--- |
| **$V_{out}$** | 전 코너·온도, 트림 후 | **1.8 V ±2%** | BGR 트림 0.22% ⊕ 분압 매칭 ⊕ EA 오프셋 |
| **$I_{load}$** | — | **50.8 µA ~ 4 mA** | §2 |
| **$I_Q$** | 무부하, 분압기 포함 (RO 제외) | **< 150 µA** | BGR 60 + EA ~20 + 분압 51 = 131 |
| **Dropout** | 4 mA (시뮬 전용) | **미확정** | 구 200/300 mV는 근거 없어 폐기. LUT 삼극관 조회로 재산출 |
| **Line reg** | VAPWR 3.0 → 3.6 V | **< 2 mV/V** | BGR 0.084 %/V 기여 0.9 mV + 루프 |
| **Load reg** | 50 µA → 4 mA DC | **< 5 mV** | $R_{out,cl} = R_{out}/(1+T)$, $T \sim 90$ dB |
| **PSRR** | 4 mA | **> 50 dB @1 kHz**<br>**> 20 dB @100 kHz** | 루프이득 형상에서 도출. 미검증 근사 |
| **PM** | 부하 {분압만, +RO, 0.5m, 2m, 4mA} × tt/ss/ff,<br>**$C_c$는 cap_low(−11%)** | **> 45°** (tt > 60°) | 설계의 8할. **경부하가 최악** |
| **과도 (고속)** | 50 → 150 µA, $t_r$ < 100 ns | **ΔV < 100 mV** | RO on/off가 실제로 이 크기 |
| **과도 (중속)** | 50 → 550 µA, $t_r$ < 100 ns | ΔV < 300 mV (정보) | 저항 싱크 0.5 mA |
| **과도 (저속)** | 50 µA → 4 mA, 램프 ≥ 20 µs | **ΔV < 50 mV** | 루프가 추종. 스위치 게이트 RC로 보장 |
| **★오버슈트** | soft-start + **부하 release 4 mA → 50 µA** | **< 1.95 V (hard)** | 01v8 부하 소자 정격 보호 |
| **노이즈** | 무부하, 10 Hz | 20~30 µV/√Hz **예상** | §5.3 — 목표가 아니라 예측 |

---

## 2. 부하 범위의 도출

### $I_{min} = 50.8\,\mu\text{A}$ — 선택이 아니라 유도값
피드백 분압기가 반드시 있어야 하고 그 전류가 곧 최소 부하다. 양쪽에서 조여진다:

| 방향 | 제약 | 한계 |
| :--- | :--- | :--- |
| R 크게 (I 작게) | 경부하 출력극 하강 → **PM 붕괴** | 25 µA면 pass의 $r_o$·$R_{div}$ 둘 다 2배 → **출력극 545 kHz → 271 kHz** |
| R 작게 (I 크게) | **$I_Q$ 예산** | 200 µA면 BGR(60 µA)보다 커진다 |

→ **25 ~ 100 µA가 창, 50 µA가 중앙.**
**$R_a$ = 4유닛(11.8 k), $R_b$ = 8유닛(23.6 k), 비 1.5 정확.** BGR 유닛저항 재사용.
**분압기가 최소부하를 겸하므로 별도 더미 부하가 불필요** — 설계 의도의 증거.

### $I_{max} = 4\,\text{mA}$ — 선언이나 창이 좁다
| 방향 | 제약 |
| :--- | :--- |
| 크게 | PDN 강하(20 mA → 0.1 V이므로 10 mA면 VAPWR 50 mV 처짐 → line reg 측정 오염), 열, gm 변동폭 |
| 작게 | 데모 가치. 레퍼런스 devstdin은 **300 µA까지밖에 특성화하지 못했다** |

**주의**: TT 아날로그 핀의 4 mA 제한은 **근거가 아니다** — 부하가 온칩이므로 무관하다.
2 mA로 줄여도 안정도는 안 쉬워진다(최악은 경부하 = $I_{min}$이 결정).

### 서사
50 µA ↔ 4 mA = **80배 비율**. 4 mA @1.8 V = 7.2 mW ≈ 50 MHz·활성률 0.15 기준 6만 게이트급
(소형 RISC-V 코어 규모). → **"상시 온 소형 디지털 도메인용 point-of-load 레귤레이터"**

---

## 3. 회로 아키텍처

### 3.1. Pass device — PMOS
`sky130_fd_pr__pfet_g5v0d10v5`. 실측 앵커(W=400/L=0.5/nf=1, 기본 geometry, VSG 1.3/VSD 1.5, tt/27):

| 항목 | 값 |
| :--- | ---: |
| $I_D$ | 3.058402 mA |
| $v_{dsat}$ | 0.2825014 V |
| $g_m$ | 14.87254 mS |
| $g_{ds}$ | 581.4709 µS |
| $C_{gg}$ | **326.1773 fF** |
| $C_{gd}$ | **2.479913 fF** |

**제약 구조**
| ID | 제약 | 방향 | 상태 |
| :---: | :--- | :---: | :--- |
| C1 | 포화 유지 | W 하한 | **binding 아님** — vdsat 0.283 V @3 mA로 여유 큼 |
| C2 | 게이트 드라이브 | — | 여유. folded cascode 출력 하한 ≈ 0.5 V → $V_{SG}$ 최대 ~2.5 V |
| C3 | **EA 출력 천장** | **W 상한** | $V_{SG}(I_{min}) > 2V_{ov,p} + $ 마진. ff/125 °C에서 마진 100~200 mV |
| C4 | **OFF 능력/리키지** | **W 상한** | $I_D(V_{SG}{=}0.4, \text{ff}, 125°C) \le 0.2 I_{min}$ |
| C5 | **$C_{gg} \le 0.15\,C_c$** | **W 상한** | $p_2 \propto C_c/(C_c{+}C_{gg})$. $C_c$=2 pF → $C_{gg}$ ≤ 300 fF → **W=400은 경계** |

→ **C1이 하한, C3/C4/C5가 상한. W=400에서 아래로 탐색할 것.**
레이아웃은 **m × (W=10 µm 핑거)** — LUT 잔여 폭 의존성(~2%)과 정석 양쪽 이유.

$C_{gg}$의 진짜 역할은 극점이 아니라 **EA 슬루율 $(C_c + C_{gg})/I_{EA}$** 이며,
오버슈트 < 1.95 V 하드 스펙이 여기 걸린다.

### 3.2. EA — PMOS 입력 Folded-Cascode ★
**근거 정정**: 과거 "출력이 상단으로 스윙해야 하니 PMOS 폴딩 → NMOS 입력" 논리는 **틀렸다.**
folded cascode의 출력 스윙은 **출력단 캐스코드 미러가 정하며 입력 극성과 무관**하다
(두 극성 모두 $[2V_{dsat,n},\, V_{DD} - 2V_{dsat,p}]$).

실제 근거는 **$V_{ICM}$ 헤드룸**이며, ss/−40 °C 실측(L=2, W=10)에서:

| | 실측 $V_{th}$ | 헤드룸 마진 |
| :--- | ---: | ---: |
| nfet ($V_{SB}$=0 / 0.15 V) | 0.9056 / 0.9656 V | **34 mV** |
| pfet ($V_{SB}$=0) | 1.1916 V | **708 mV** |

thick-ox $V_{th}$가 0.9~1.2 V인 공정에서 $V_{ICM}$ = 1.2 V는 3.3 V mid-rail(1.65 V)보다 아래이므로
**처음부터 PMOS 입력 영역**이었다.

**부수 이점**: tail 캐스코드 가능(→ PSRR), 자체 nwell이라 바디효과 없음, 1/f 낮음, LUT 안에 머묾.
**인정한 반대 논거**: NMOS tail은 접지에 붙어 원리상 PSRR 유리하나, PMOS는 캐스코드 가능·NMOS는
헤드룸상 불가이므로 실질 역전.

**역할별 gm/Id 배정 (Task 3 출발점)**
| 역할 | gm/Id | L | 근거 |
| :--- | ---: | ---: | :--- |
| 입력쌍 (PMOS) | 15~18 | 1~2 µm | gm 효율 최대 → UGF 확보. 노이즈 유리 |
| 폴딩 전류원 (NMOS) | **5~7** | 2~4 µm | gm을 낮게 유지해야 미러 미스매치의 입력환산 오프셋 축소 |
| cascode | 8~12 | 0.5~1 µm | $v_{dsat}$ 헤드룸과 속도 절충 |
| tail (PMOS, 캐스코드) | 5~7 | 4 µm | 전류 정확도, 낮은 $g_{ds}$ |

> **원칙**: 신호 경로는 gm/Id 높게, 잡음·오프셋 경로는 gm/Id 낮게.

### 3.3. 보상
표준 Miller + $R_z$ 1순위. $C_{gd}$ 2.5 fF로 pass 자체 Miller가 무시 가능하므로 RHP zero 위협이 낮다.
Ahuja는 PSRR 개선 옵션.

**$C_c$ = 12.8 pF는 레퍼런스(devstdin) 복사값이며 우리 설계값이 아니다. Task 2의 출력이지 입력이 아니다.**

---

## 4. ★ 과도응답 물리 한계와 Task 2 목적함수

$$\Delta V_{droop} \approx \frac{I_{step}}{2\pi \cdot UGF \cdot C_{out}}$$

$C_{out}$ = 30 pF, UGF = 10 MHz(공격적)에서 $Z_{peak}$ = 530 Ω:

| 스텝 | 드룹 |
| :--- | ---: |
| 100 µA | 53 mV |
| 500 µA | 265 mV |
| **4 mA** | **2.1 V — 붕괴** |

**4 mA 고속 스텝은 온칩 캡만으로 물리적으로 불가능하다.**
외부 캡도 무용지물 — ua 경로 500 Ω × 1 µF의 코너가 318 Hz.

이것은 결함이 아니라 **capless + 소면적의 근본 한계**다. 상용 "capacitor-free"는 *안정도*에
외부 캡이 불필요하다는 뜻이지 과도까지 커버한다는 뜻이 아니다.
→ **부하 슬루를 우리가 소유하므로 스펙을 3단 계층화**(§1).

### Task 2 목적함수 (최종형)
> **면적 제약 하에서 `C_out × UGF` 최대화.** 단
> (a) $C_{gg} \le 0.15\,C_c$ 극점 조건,
> (b) 슬루율 $(C_c + C_{gg})/I_{EA}$가 오버슈트 < 1.95 V를 만족,
> (c) $C_c$ 축소가 HF PSRR을 개선하나 LF PSRR·과도와 충돌 → 주파수대별 분리 최적화,
> (d) $C_{out}$은 **MiM 적층(4.0 fF/µm²) + over-active 배치** 전제.

원 목적함수였던 "$C_c$ 최소화"는 **대체됨.**

### $C_{out}$ 소자 선택 — MOS cap 기각
thin-ox MOS cap의 절대최대가 ~1.95 V인데 **우리 오버슈트 하드 스펙이 정확히 1.95 V**다.
즉 스펙 위반 시 **디캡이 가장 먼저 죽는 소자**가 된다.

→ **MiM 적층 채택.** `camimc`가 `cap_mim_m3_1`·`m3_2` 동일이므로 적층 시 4.0 fF/µm²,
금속 유전체라 항복 여유가 크고, **met3~met4라 액티브 위에 배치 가능해 실면적 벌금이 거의 없다.**
(주의: `cap2m`이 met5 인접이므로 TT의 met5 금지와 간섭 확인 필요. m3_1 단층은 2.0 fF/µm²로 안전)

### HF PSRR과 $C_c$의 관계
HF에서 pass 게이트는 $C_{gs}$(→VAPWR)와 $C_c + C_{gd}$(→출력 = AC 접지)의 용량 분압으로 결정된다:

$$\text{VAPWR 추종률} = \frac{C_{gs}}{C_{gs} + C_c + C_{gd}}$$

$C_{gs}$ ≈ 200 fF 기준: $C_c$ = 2 pF → 9%, $C_c$ = 0.5 pF → **29%**.
추종률이 높을수록 $V_{SG}$가 일정해져 **HF PSRR 개선.**
→ **"$C_c$ 축소 = HF PSRR 개선"이 성립.** Ahuja가 PSRR에 좋은 이유도 동일하다.
**단 $C_c$ 축소는 UGF를 줄이므로 LF PSRR·과도와는 여전히 트레이드.**

---

## 5. 부하 블록 및 측정 아키텍처

### 5.1. 온칩 부하
아날로그 핀으로 부하를 끌면 TT 경로 저항이 측정을 지배한다 — 레퍼런스 devstdin은
출력 300 µA에서 50 mV 강하(실효 160 Ω)를 측정했는데 **LDO 자체는 ~1 mV만 예상**되는 값이었다.
**→ 부하는 전부 온칩, `ua`는 전압 sense 전용.**

1. **RO 데모 부하** — 일반 인버터 링(01v8), 1단은 NAND(enable), + 분주기 → `uo_out[0]`
   - **current-starved 불채택**: 그 구조는 f가 기준전류에 고정되어 **공급 전압에 둔감**한데,
     우리는 **f를 LDO 출력 품질의 프록시로 쓰는 것이 데모의 핵심**이므로 민감도가 기능이다
   - **설계 공간이 직교**: $I_{avg} = NCV/(2N t_{pd}) = CV/(2t_{pd}) \approx I_{D,sat}$(1단)이라 **N이 소거된다**
     → **W(일괄 스케일) → $I_{RO}$**, **N(단수) → $f \propto 1/N$**
   - **분주기 필수**: 패드 최대 출력 주파수 33 MHz. 목표 $f_{osc}$ 100~200 MHz → /16~/128
   - **첫 2단은 TSPC divide-by-2**, 이후 표준셀 DFF
   - 탭 노드는 작은 버퍼 + 나머지 노드에 더미 부하 (듀티 왜곡 방지)
2. **특성 부하** — 저항+스위치 싱크 3종 (0.9 k / 1.8 k / 3.6 k = 2 / 1 / 0.5 mA @1.8 V,
   01v8 스위치, `ui_in[7:5]`). 전류밀도 때문에 `mult` 분할 필요(면적은 무시 가능)

### 5.2. 측정 — `ua[0]`의 함정
BGR 출력 임피던스가 **121 k**이므로 10 MΩ 입력 DMM으로 재면 **1.2% 분압 오차**
(트림 LSB 0.44%의 3배). 레퍼런스 `cw_vref`는 이 문제로 테스트 버퍼를 내장하고 외부 바이어스 핀까지 썼다.

**우리 해법: 버퍼 없이 SMU/전위계(입력 >100 MΩ)로 측정.** 비용 0, 오차 0.001%.
`ua[1]`(= 1.5 × $V_{ref}$)와의 교차검증이 캘리브레이션을 대신한다.

### 5.3. 노이즈 항의 지위
§1의 20~30 µV/√Hz는 **목표가 아니라 구조적 예측**이다.
레퍼런스 실측이 보여준 구조는 "LDO 자체 1.8~1.9 µV/√Hz vs 밴드갭 13~19 µV/√Hz × 이득 1.5"이며,
**LDO EA를 아무리 조용히 만들어도 BGR × 1.5가 바닥**이라는 뜻이다.
절대치는 우리 BGR 노이즈 측정(PEX 후) 이후 확정한다. **PASS/FAIL 판정 대상이 아니다.**

---

## 6. TinyTapeout 제약 (공식 문서 확인분)

| 항목 | 값 |
| :--- | :--- |
| 전원 핀 | VGND / **VDPWR = 1.8 V 디지털 코어** / **VAPWR = 3.3 V 아날로그**(선택) |
| 3.3 V 사용 시 | `tt_analog_*_3v3.def` 템플릿 필수 + `info.yaml`에 `uses_3v3: true` |
| PDN | **20 mA 소모 시 0.1 V 강하** → 우리 4 mA면 VAPWR ~20 mV 처짐 |
| 아날로그 핀 경로 | 저항 < 500 Ω, 커패시턴스 < 5 pF, 최대 전류 4 mA |
| 아날로그 핀 | `ua[0]`~`ua[5]`만, **0번부터 순서대로** |
| 디지털 출력 패드 | `sky130_ef_io_gpiov2_pad`, **최대 출력 33 MHz** (입력 66 MHz) |
| 금지 | **metal 5 사용 불가** |
| 필수 | 미구동 `uo_out`/`uio_out`/`uio_oe`는 GND 연결 |
| 면적 | 1x2 = 160 × 225 µm (3v3는 약간 좁음 — **DIEAREA로 확정 필요**) |

**귀결**: 디지털 도메인은 **1.8 V 고정**. 보드에서 3.3 V를 인가해도 TT 내부 로직(VDPWR)을 거쳐
오므로 우리에게 도착하는 건 1.8 V CMOS다. 이 때문에 트림 스위치를 **R2 접지단**에 배치해
1.8 V 직결로 구동하며, **레벨시프터가 불필요**하다.

---

## 7. 레퍼런스 프로젝트 (조건 대조용)

| 분류 | 예 | 쓸 것 | 쓰면 안 되는 것 |
| :--- | :--- | :--- | :--- |
| **동일 환경 실리콘** | devstdin TT08, cw_vref | **달성 가능치의 현실 기준** | — |
| **상용 데이터시트** | TI TLV713 급 | **스펙 항목·측정 조건의 어휘** | 목표 수치 (전용공정 + ATE 개별 트림 산물) |
| **우리 BGR 실측** | $V_{ref}$ 1.18659 V, TC 7.5 ppm | **이미 확정된 바닥** | — |

**devstdin TT08** (`tt08-ldo-bg-ref-osc`, 로컬에 클론 있음)
- 무부하 1.842 V @20 °C. 50 °C 구간 드리프트 10 mV인데 typical 시뮬은 2 mV만 설명
  → **MC 코너로만 해명됨. PEX + MC 필수**
- 출력 임피던스 실측 160 Ω = 대부분 TT 경로 (§5.1의 근거)
- 노이즈 구조 (§5.3의 근거)
- RO 5단 current-starved, 6.25 MHz. 기준전류 편차 영향을 평가하지 못해
  시뮬 8.1~10.8 MHz vs 실측 5.2~6.25 MHz로 어긋남

**cw_vref (ttsky25a)** — 측정 아키텍처 참고. 8-bit 트림, 테스트 버퍼 + loop-through 캘리브레이션
