# 04_LDO_스펙.md (LDO 목표 사양 및 아키텍처 확정서)

최종 갱신: 2026-07 — EA 극성 재판정, C_byp 신호결합 대책, PG 급전 레일, thin-ox LUT 기반 RO 전략 반영

### TL;DR (4줄 요약)
1. VAPWR 3.3 V → 1.8 V, 부하 50.8 µA ~ 2.10 mA의 point-of-load LDO 목표 사양과 그 도출 근거를 규정합니다.
2. **PMOS 입력 Folded-Cascode EA + PMOS pass** 구조이며, 극성 근거는 V_ICM 헤드룸 실측입니다(과거 "출력 스윙" 논리는 폐기).
   BGR-EA 신호결합으로 인한 위상마진 붕괴는 $C_{byp}$(§3.4)로, power-good은 VAPWR/thick-ox 급전(§6.1)으로 방어합니다.
3. capless + 소면적의 과도응답 물리 한계를 계산하여 스펙을 3단 계층화하고, Task 2의 목적함수를 `C_out × UGF` 최대화로 확정했습니다.
4. RO는 최소 L + 명시적 MiM 부하캡으로 전류·주파수·단수를 직교시킵니다 — thin-ox gm/Id LUT(`lut/README.md`)가 근거입니다.

> **판정 근거와 대체 이력은 `designs/bgr_ldo/ldo/DECISIONS.md`가 정본**입니다.
> 이 문서는 결론의 요약이며, 충돌 시 DECISIONS.md를 따르십시오.

---

## 1. 목표 스펙

| 항목 | 조건 | 목표 | 근거 |
| :--- | :--- | :--- | :--- |
| **$V_{out}$** | 전 코너·온도, 트림 후 | **1.8 V ±2%** | BGR 트림 0.22% ⊕ 분압 매칭 ⊕ EA 오프셋 |
| **$I_{load}$** | — | **50.8 µA ~ 2.10 mA** | §2 (Task 5 슬루 드라이버로 확정) |
| **$I_Q$** | 무부하, 분압기 포함 (RO 제외) | **< 150 µA** | BGR 60 + EA ~20 + 분압 51 = 131 |
| **Dropout** | 2.10 mA (시뮬 전용) | **미확정** | 구 200/300 mV는 근거 없어 폐기. LUT triode 조회로 재산출 |
| **Line reg** | VAPWR 3.0 → 3.6 V | **< 2 mV/V** | BGR 0.084 %/V 기여 0.9 mV + 루프 |
| **Load reg** | 50 µA → 2.10 mA DC | **< 5 mV** | $R_{out,cl} = R_{out}/(1+T)$, $T \sim 90$ dB |
| **PSRR** | 2.10 mA | **> 50 dB @1 kHz**<br>**> 20 dB @100 kHz** | 루프이득 형상에서 도출. 미검증 근사 |
| **PM** | 부하 {분압만, +RO, 0.5m, 2m, 2.10mA} × tt/ss/ff,<br>**$C_c$는 cap_low(−11%)** | **> 45°** (tt > 60°) | 설계의 8할. **경부하가 최악** |
| **과도 (고속)** | 50 → 150 µA, $t_r$ < 100 ns | **ΔV < 100 mV** | RO on/off = 95 µA 스텝(실측). 슬루 제한 없는 유일한 고속 스텝 |
| **과도 (중속)** | 50 → 550 µA, $t_r$ < 100 ns | ΔV < 300 mV (정보) | 저항 싱크 0.5 mA |
| **과도 (저속)** | 50 µA → 2.10 mA, 램프 ≥ 20 µs | **ΔV < 50 mV** | 루프가 추종. 스위치 게이트 RC로 보장 |
| **★오버슈트** | soft-start + **부하 release 2.10 mA → 50 µA** | **< 1.95 V (hard)** | 01v8 부하 소자 정격 보호 |
| **노이즈** | 무부하, 10 Hz | 20~30 µV/√Hz **예상** | §5.3 — 목표가 아니라 예측 |

---

## 2. 부하 범위의 도출

### $I_{min} = 50.8\,\mu\text{A}$ — 선택이 아니라 유도값
피드백 분압기가 반드시 있어야 하고 그 전류가 곧 최소 부하다. 양쪽에서 조여진다:

| 방향 | 제약 | 한계 |
| :--- | :--- | :--- |
| R 크게 (I 작게) | 경부하 출력극 하강 → **PM 붕괴** | 25 µA면 pass의 $r_o$·$R_{div}$ 둘 다 2배 → **출력극 545 kHz → 271 kHz** |
| R 작게 (I 크게) | **$I_Q$ 예산** | 200 µA면 BGR(60 µA)보다 커진다 |

→ **25 ~ 100 µA가 창, 50 µA가 중앙.**
**$R_a$ = 4유닛(11.8 k), $R_b$ = 8유닛(23.6 k), 비 1.5 정확.** BGR 유닛저항 재사용.
**분압기가 최소부하를 겸하므로 별도 더미 부하가 불필요** — 설계 의도의 증거.

### $I_{max} = 2.10\,\text{mA}$ — Task 5 슬루 드라이버 확정치

구 스펙 4 mA는 온칩 캡만으로 물리적으로 불가능함(§4 과도 물리 한계)이
확인된 뒤, 슬루 드라이버로 실현 가능한 값을 재확정했다.

**A안 채택**: 부하 싱크를 3브랜치(0.5/1/2 mA)에서 **단일 2 mA 브랜치**로
통합하고, 슬루 드라이버(직렬 폴리 저항 + MiM 캡, RC)를 1벌만 배치했다.
브랜치별 드라이버 3벌은 면적 대비 이득이 작아 기각.

$$I_{max} = I_{div}(46\,\mu\text{A}) + I_{RO}(60\,\mu\text{A}) + I_{snk}(1993\,\mu\text{A}) = 2.10\,\text{mA}$$

| | 구 스펙 | **신 스펙** |
| :--- | ---: | ---: |
| $I_{max}$ | 4 mA | **2.10 mA** |
| 부하 범위비 | 80배 | **46배** |
| 최대 전력 (@1.8V) | 7.2 mW | **3.8 mW** |

**서사 갱신**: "상시 온 소형 디지털 도메인용 point-of-load 레귤레이터"는
유지하되 규모 표현을 "6만 게이트급"에서 **"3만 게이트급"**으로 낮춘다.

**검증(tt/27, ll/125, Task 5)**: 게이트 슬루 엣지 1.85 µs 양방향(대칭
0.69%), release 오버슈트 마진 104~129 mV(1.95 V 대비), 인가 시 PG 문턱
대비 마진 82.5~104 mV, 로드 레귤레이션 92 µV(스펙 5 mV의 1.8%), 폐루프
AC 무간섭(PM/GM 변화 <0.1).

---

## 3. 회로 아키텍처

### 3.1. Pass device — PMOS
`sky130_fd_pr__pfet_g5v0d10v5`. 실측 앵커(W=400/L=0.5/nf=1, 기본 geometry, VSG 1.3/VSD 1.5, tt/27):

| 항목 | 값 |
| :--- | ---: |
| $I_D$ | 3.058402 mA |
| $v_{dsat}$ | 0.2825014 V |
| $g_m$ | 14.87254 mS |
| $g_{ds}$ | 581.4709 µS |
| $C_{gg}$ | **326.1773 fF** |
| $C_{gd}$ | **2.479913 fF** |

**제약 구조**
| ID | 제약 | 방향 | 상태 |
| :---: | :--- | :---: | :--- |
| C1 | 포화 유지 | W 하한 | **binding 아님** — vdsat 0.283 V @3 mA로 여유 큼 |
| C2 | 게이트 드라이브 | — | 여유. folded cascode 출력 하한 ≈ 0.5 V → $V_{SG}$ 최대 ~2.5 V |
| C3 | **EA 출력 천장** | **W 상한** | $V_{SG}(I_{min}) > 2V_{ov,p} + $ 마진. ff/125 °C에서 마진 100~200 mV |
| C4 | **OFF 능력/리키지** | **W 상한** | $I_D(V_{SG}{=}0.4, \text{ff}, 125°C) \le 0.2 I_{min}$ |
| C5 | **$C_{gg} \le 0.15\,C_c$** | **W 상한** | $p_2 \propto C_c/(C_c{+}C_{gg})$. $C_c$=2 pF → $C_{gg}$ ≤ 300 fF → **W=400은 경계** |

→ **C1이 하한, C3/C4/C5가 상한. W=400에서 아래로 탐색할 것.**
레이아웃은 **m × (W=10 µm 핑거)** — LUT 잔여 폭 의존성(~2%)과 정석 양쪽 이유.

**`m`과 `nf`의 관계 (lut/README.md §9.8)**: 게이트 저항에서 둘은 동등하다
($R_g$는 핑거 하나의 폭으로 정해지고, 병렬 개수가 m이든 nf든 같게 나눈다).
차이는 접합 용량뿐이며(`W=nf×핑거`가 확산을 공유해 `ad/as`가 약 절반), 우리 노드에서는
무시 가능하다(pass 드레인 = LDO 출력, MiM 30 pF 대비 ~58 fF 차이는 0.2%).
**LUT 정확도 관점에서는 `m × (W=10, nf=1)`이 유리**하다 — BSIM4가 핑거 폭(W/nf)으로
bin을 고르므로 이 구성이 기준폭에 정확히 안착한다. `nf`를 실제로 쓰는 이유는 면적·종횡비 같은
**레이아웃 사유**이며 Task 8에서 확정한다. 지금 스키매틱을 다시 그릴 필요는 없다.

$C_{gg}$의 진짜 역할은 극점이 아니라 **EA 슬루율 $(C_c + C_{gg})/I_{EA}$** 이며,
오버슈트 < 1.95 V 하드 스펙이 여기 걸린다.

### 3.2. EA — PMOS 입력 Folded-Cascode ★
**근거 정정**: 과거 "출력이 상단으로 스윙해야 하니 PMOS 폴딩 → NMOS 입력" 논리는 **틀렸다.**
folded cascode의 출력 스윙은 **출력단 캐스코드 미러가 정하며 입력 극성과 무관**하다
(두 극성 모두 $[2V_{dsat,n},\, V_{DD} - 2V_{dsat,p}]$).

실제 근거는 **$V_{ICM}$ 헤드룸**이며, ss/−40 °C 실측(L=2, W=10)에서:

| | 실측 $V_{th}$ | 헤드룸 마진 |
| :--- | ---: | ---: |
| nfet ($V_{SB}$=0 / 0.15 V) | 0.9056 / 0.9656 V | **34 mV** |
| pfet ($V_{SB}$=0) | 1.1916 V | **708 mV** |

thick-ox $V_{th}$가 0.9~1.2 V인 공정에서 $V_{ICM}$ = 1.2 V는 3.3 V mid-rail(1.65 V)보다 아래이므로
**처음부터 PMOS 입력 영역**이었다.

**부수 이점**: tail 캐스코드 가능(→ PSRR), 자체 nwell이라 바디효과 없음, 1/f 낮음, LUT 안에 머묾.
**인정한 반대 논거**: NMOS tail은 접지에 붙어 원리상 PSRR 유리하나, PMOS는 캐스코드 가능·NMOS는
헤드룸상 불가이므로 실질 역전.

**역할별 gm/Id 배정 (Task 3 출발점)**
| 역할 | gm/Id | L | 근거 |
| :--- | ---: | ---: | :--- |
| 입력쌍 (PMOS) | 15~18 | 1~2 µm | gm 효율 최대 → UGF 확보. 노이즈 유리 |
| 폴딩 전류원 (NMOS) | **5~7** | 2~4 µm | gm을 낮게 유지해야 미러 미스매치의 입력환산 오프셋 축소 |
| cascode | 8~12 | 0.5~1 µm | $v_{dsat}$ 헤드룸과 속도 절충 |
| tail (PMOS, 캐스코드) | 5~7 | 4 µm | 전류 정확도, 낮은 $g_{ds}$ |

> **원칙**: 신호 경로는 gm/Id 높게, 잡음·오프셋 경로는 gm/Id 낮게.

### 3.3. 보상
표준 Miller + $R_z$ 1순위. $C_{gd}$ 2.5 fF로 pass 자체 Miller가 무시 가능하므로 RHP zero 위협이 낮다.
Ahuja는 PSRR 개선 옵션.

**$C_c$ = 12.8 pF는 레퍼런스(devstdin) 복사값이며 우리 설계값이 아니다. Task 2의 출력이지 입력이 아니다.**

### 3.4. ★ $C_{byp}$ — BGR-EA 신호 결합 억제 (2 pF, 잠정값)

folded cascode 단일단 구동 시 발생하는 위상마진 붕괴를 막기 위한 소자다.

**메커니즘**: LDO 피드백($V_{inp}$)이 흔들리면 입력 PMOS 하나(M1)가 소스 팔로워처럼
동작해 공통 소스 노드($V_{tail}$)를 흔든다. 이 흔들림이 반대쪽 입력 PMOS(M2)의
$C_{gs2}$를 타고 **BGR 출력 노드**($V_{inn}$)로 건너간다. BGR 출력 임피던스가
크므로(121 kΩ) $V_{inn}$이 함께 흔들려 차동 입력 $(V_{inp}-V_{inn})$이 부분 상쇄되고,
**위상마진이 21° 무너진다** ($41.4° \rightarrow$ 붕괴 전 값).

**해결**: $V_{inn}$ 노드에 $C_{byp}$를 달아 고주파 전압 분배비
$C_{gs2}/(C_{gs2}+C_{byp})$를 **0.49 → 0.149**로 낮춰 결합 전압을 죽인다.
$C_{gs2} \approx 358\,\text{fF}$ 기준 $C_{byp} = 2\,\text{pF}$에서 **위상마진 63.5°로 회복**.

**부수 효과**: BGR 고주파 노이즈의 저역통과 필터링. 또한 기동 램프 지연
$\tau = R_{out}C_{byp} \approx 248\,\text{ns}$가 형성되어 소프트스타트를 보조한다
(상용 LDO의 NR/BYP 핀 커패시터와 원리가 같다). 단, $\tau$가 짧아 이것만으로
오버슈트 < 1.95 V 하드 스펙을 만족하지 못한다 — **별도 soft-start가 여전히 필요**하다(Task 5).

**주의 2건**
1. $C_{gs2} = 358\,\text{fF}$는 **intrinsic 값일 가능성이 크다** (LUT 규약, D6).
   실효값은 $+W \cdot C_{GSO}$ 보정이 필요하며, 입력쌍이 $W \approx 110\,\mu\text{m}$급이면
   오버랩 $\approx 21\,\text{fF}$가 더해져 분압비가 0.149 → 0.157로 약 5% 나빠진다.
   결론은 유지되나 정밀 검증 시 반영할 것.
2. $C_{byp}$는 **잠정값**이다. Task 3에서 입력쌍을 gm/Id로 재사이징하면 $C_{gs2}$가
   바뀌므로, 절대값보다 **$C_{byp} \approx 6 \times C_{gs2,\text{eff}}$ 비율**로 기억할 것.
   면적은 MiM 적층(4 fF/µm²) 기준 ~500 µm²로 §3.1 예산 안이다.

---

## 4. ★ 과도응답 물리 한계와 Task 2 목적함수

$$\Delta V_{droop} \approx \frac{I_{step}}{2\pi \cdot UGF \cdot C_{out}}$$

$C_{out}$ = 30 pF, UGF = 10 MHz(공격적)에서 $Z_{peak}$ = 530 Ω:

| 스텝 | 드룹 |
| :--- | ---: |
| 100 µA | 53 mV |
| 500 µA | 265 mV |
| **2.10 mA** | **2.1 V — 붕괴** |

**2.10 mA 고속 스텝은 온칩 캡만으로 물리적으로 불가능하다.**
외부 캡도 무용지물 — ua 경로 500 Ω × 1 µF의 코너가 318 Hz.

이것은 결함이 아니라 **capless + 소면적의 근본 한계**다. 상용 "capacitor-free"는 *안정도*에
외부 캡이 불필요하다는 뜻이지 과도까지 커버한다는 뜻이 아니다.
→ **부하 슬루를 우리가 소유하므로 스펙을 3단 계층화**(§1).

### Task 2 목적함수 (최종형)
> **면적 제약 하에서 `C_out × UGF` 최대화.** 단
> (a) $C_{gg} \le 0.15\,C_c$ 극점 조건,
> (b) 슬루율 $(C_c + C_{gg})/I_{EA}$가 오버슈트 < 1.95 V를 만족,
> (c) $C_c$ 축소가 HF PSRR을 개선하나 LF PSRR·과도와 충돌 → 주파수대별 분리 최적화,
> (d) $C_{out}$은 **MiM 적층(4.0 fF/µm²) + over-active 배치** 전제.

원 목적함수였던 "$C_c$ 최소화"는 **대체됨.**

### $C_{out}$ 소자 선택 — MOS cap 기각
thin-ox MOS cap의 절대최대가 ~1.95 V인데 **우리 오버슈트 하드 스펙이 정확히 1.95 V**다.
즉 스펙 위반 시 **디캡이 가장 먼저 죽는 소자**가 된다.

→ **MiM 적층 채택.** `camimc`가 `cap_mim_m3_1`·`m3_2` 동일이므로 적층 시 4.0 fF/µm²,
금속 유전체라 항복 여유가 크고, **met3~met4라 액티브 위에 배치 가능해 실면적 벌금이 거의 없다.**
(주의: `cap2m`이 met5 인접이므로 TT의 met5 금지와 간섭 확인 필요. m3_1 단층은 2.0 fF/µm²로 안전)

### HF PSRR과 $C_c$의 관계
HF에서 pass 게이트는 $C_{gs}$(→VAPWR)와 $C_c + C_{gd}$(→출력 = AC 접지)의 용량 분압으로 결정된다:

$$\text{VAPWR 추종률} = \frac{C_{gs}}{C_{gs} + C_c + C_{gd}}$$

$C_{gs}$ ≈ 200 fF 기준: $C_c$ = 2 pF → 9%, $C_c$ = 0.5 pF → **29%**.
추종률이 높을수록 $V_{SG}$가 일정해져 **HF PSRR 개선.**
→ **"$C_c$ 축소 = HF PSRR 개선"이 성립.** Ahuja가 PSRR에 좋은 이유도 동일하다.
**단 $C_c$ 축소는 UGF를 줄이므로 LF PSRR·과도와는 여전히 트레이드.**

---

## 5. 부하 블록 및 측정 아키텍처

### 5.1. 온칩 부하
아날로그 핀으로 부하를 끌면 TT 경로 저항이 측정을 지배한다 — 레퍼런스 devstdin은
출력 300 µA에서 50 mV 강하(실효 160 Ω)를 측정했는데 **LDO 자체는 ~1 mV만 예상**되는 값이었다.
**→ 부하는 전부 온칩, `ua`는 전압 sense 전용.**

1. **RO 데모 부하** — 일반 인버터 링(01v8) + 분주기 → `uo_out[0]`. **VDDC 급전**
   (f를 LDO 출력 품질의 프록시로 쓰는 것이 데모의 핵심이므로 링은 반드시 LDO 출력에서
   전원을 받는다).
   - **current-starved 불채택**: 그 구조는 f가 기준전류에 고정되어 **공급 전압에 둔감**한데,
     우리는 **f를 LDO 출력 품질의 프록시로 쓰는 것이 데모의 핵심**이므로 민감도가 기능이다
   - **설계 공간이 직교**: $I_{avg} = NCV/(2N t_{pd}) = CV/(2t_{pd}) \approx I_{D,sat}$(1단)이라 **N이 소거된다**
     → **W(일괄 스케일) → $I_{RO}$**, **N(단수) → $f \propto 1/N$**
   - **★ L은 최소(표준셀 호환), f는 명시적 부하캡으로 제어 — L을 f 제어에 안 쓰는 이유**:
     $L$을 키우면 $t_{pd} \propto L^2$로 늘지만 $I \approx I_{D,sat}(\text{1단}) \propto 1/L$도
     함께 줄어 RO가 최소부하 역할을 못 하게 된다. 반대로 최소 $L$(0.15 µm)은 $t_{pd}$가
     수십 ps대라 5단에서 f가 **GHz대**로 치솟아 분주기 첫단이 못 받는다.
     **명시적 MiM 부하캡**을 노드마다 달면 $t_{pd} \propto C$이지만 $I=CV/(2t_{pd})$라
     **전류는 불변** — W(전류)·C(주파수)·N(단수)이 완전히 직교한다.

   **확정 사이징** (tt/27, 단독 TB 실측)

   | 블록 | 소자 | W / L | m |
   | :--- | :--- | :--- | ---: |
   | 인버터 nfet ×10 | `nfet_01v8` | 0.42 / 0.15 | 1 |
   | 인버터 pfet ×10 | `pfet_01v8` | 1.0 / 0.15 | 1 |
   | NAND nfet (직렬 2) | `nfet_01v8` | 1.0 / 0.15 | 1 |
   | NAND pfet (병렬 2) | `pfet_01v8` | 1.0 / 0.15 | 1 |
   | 부하캡 ×11 | `cap_mim_m3_1` | W=L=4.27 µm (XC10만 4.17) | 1 |

   구성: NAND 1단(enable) + 인버터 10단 = **11단 홀수**, 탭 버퍼 2단(부하캡 없음).
   부하캡은 셀 안이 아니라 탑레벨에 배치 — f 튜닝의 유일한 노브이므로 한눈에 보이게 한다.

   **실측 (tt/27, 이상 1.8 V 전원, 분주기 입력 부하 5 fF 포함)**

   | 항목 | 값 |
   | :--- | ---: |
   | $f_{osc}$ | **110.89 MHz** |
   | $I_{RO}$ | **95.07 µA** |
   | duty (RO_OUT) | 49.18 % |
   | RO_OUT 엣지(10-90%) | 90.8 ps |
   | 스윙 | −0.020 ~ 1.843 V |
   | $C_{node}$ (역산) | 42.8 fF (설계 38.8 대비 +10%) |

   **★ $I_{sat}$은 실효 구동전류를 1.6배 과대평가한다.** LUT 조회 $I_{sat}$ = 153.6 µA
   기준으로 f=200 MHz를 설계했으나 실측 110.9 MHz. $f = I/(2NCV)$에 실효전류 비 0.61과
   용량 비 1.10을 넣으면 200×0.61/1.10 = 111 MHz로 실측과 일치한다. 전이 중 소자가
   포화를 벗어나 삼극관으로 들어가므로 평균 구동전류가 $I_{sat}$보다 작다
   (alpha-power law 보정 계수 0.6~0.7과 정합).

   **★ 직교성 실증**: 분주기 입력 부하 5 fF를 추가하자 $I_{RO}$가 94.08 → 95.07 µA로
   **+0.99 µA** 증가했다. 전하 모델 예측 $C \cdot V \cdot f$ = 5 fF × 1.8 V ×
   110.9 MHz = **0.998 µA**와 소수점까지 일치. $I = NCVf$ 관계가 정확히 성립하며,
   따라서 부하캡을 바꿔도 $I_{RO}$는 불변임이 실측으로 확인되었다.

   **부하캡 조정 불요 확정**: f 110.9 MHz는 목표 범위(100~200 MHz) 안이고,
   $I_{RO}$ 95 µA는 §1 고속 과도 스펙("50 → 150 µA" = 100 µA 스텝)과 거의 일치한다.

1.1 **분주기 (÷16)** — `sky130_fd_sc_hd__dfxbp_1` 4개 리플 카운터.
   각 단이 $\bar{Q}$를 $D$로 되먹여 T-플립플롭(÷2), 4단 = ÷16.

   **★ VDPWR(1.8 V) 급전** — 링과 전원 도메인을 분리한다. 이유:
   (a) 링만 LDO 부하가 되어 $I_{RO}$가 깨끗하게 정의됨, (b) 분주기 스위칭 노이즈가
   LDO 출력에 실리지 않음, (c) `uo_out`이 원래 VDPWR 도메인.
   도메인 경계: RO_OUT 스윙 1.78 V(VDDC) → 분주기 문턱 0.9 V(VDPWR/2)로 여유 큼.

   **실측 (tt/27, CK 110.889 MHz 1.78 V 스윙, 출력 부하 5 fF)**: 각 단 분주비
   **2.0000**, DIV_OUT **6.931 MHz**, 스윙 −0.033 ~ 1.872 V, 평균 0.934 V.

   **TSPC 불채택**: 당초 200 MHz를 전제해 첫 2단을 TSPC 커스텀으로 계획했으나,
   실측 $f_{osc}$가 110.9 MHz로 나와 표준셀 DFF가 전 단을 여유롭게 받는다
   (ff 코너 160 MHz에서도 6.25 ns 주기). 커스텀 설계·검증 부담을 제거했다.

   **패드 여유**: 공칭 6.93 MHz, ff 코너 최악 10.1 MHz — 패드 상한 33 MHz 대비 3.3배.

   **주의(구현)**: xschem 심볼 `dfxbp_1.sym`은 `type=primitive` 블랙박스라 하위
   스키매틱이 없고 서브서킷 정의가 넷리스트에 딸려오지 않는다. 시뮬 시
   `libs.ref/sky130_fd_sc_hd/spice/sky130_fd_sc_hd.spice`에서 해당 `.subckt`를
   추출해 `.include` 해야 한다. 전원은 그래픽 핀이 아니라 **인스턴스 속성**
   (`VPWR`/`VGND`/`VPB`/`VNB`)으로 넷 이름을 지정한다
   (VPB = nwell 탭 → 전원, VNB = psub 탭 → 접지).

2. **특성 부하** — 단일 2 mA 싱크 + 슬루 드라이버(RC) 1벌.
   01v8 스위치 (`ui_in[7]`).

   **★ 슬루 드라이버 버퍼는 VDDC(LDO 출력) 급전이다.** 초기안은 VDPWR이었으나
   VDDC로 변경했다. 이유:
   (a) **자기 순서화** — LDO가 서야 싱크가 켜진다. 기동 중 `ui_in[7]`=1이어도
       VDDC가 낮으면 버퍼가 동작하지 않아 부하 충돌이 원천 차단된다.
   (b) **논리 정합** — LDO 오버슈트를 막는 회로가 LDO 자신의 출력으로 동작한다.
   비용: 정상 상태 소비 0(CMOS), 전이 중 $R_{slew}$ 통과 최대 6.0 µA(τ=0.8 µs 감쇠,
   총 전하 ~4 pC), $R_{on}$ 22.88 → 23.04 Ω(+0.7%).

   **VDDC 급전 판 실측 (tt/27)**

   | 항목 | VDPWR 판 | VDDC 판 |
   | :--- | ---: | ---: |
   | `vg_snk` high | 1.800 V | 1.7799 V |
   | 싱크 $I_D$ | 1.9706 mA | 1.9702 mA |
   | $R_{on}$ | 22.88 Ω | 23.04 Ω |
   | 슬루 rise / fall | 1.852 / 1.839 µs | 1.869 / 1.839 µs |
   | $V_{min}$(인가) | 1.719 V | 1.723 V |
   | $V_{max}$(release) | 1.821 V | 1.821 V |
   | `net6` / `VNB1` | — | 1.186591 / 1.111989 (불변) |

   RC 시상수가 수동 소자(295.0 kΩ × 2.727 pF)로 정해지므로 공급 전압이 3.2% 흔들려도
   엣지는 0.9%만 변한다.

### 5.2. 측정 — `ua[0]`의 함정
BGR 출력 임피던스가 **121 k**이므로 10 MΩ 입력 DMM으로 재면 **1.2% 분압 오차**
(트림 LSB 0.44%의 3배). 레퍼런스 `cw_vref`는 이 문제로 테스트 버퍼를 내장하고 외부 바이어스 핀까지 썼다.

**우리 해법: 버퍼 없이 SMU/전위계(입력 >100 MΩ)로 측정.** 비용 0, 오차 0.001%.
`ua[1]`(= 1.5 × $V_{ref}$)와의 교차검증이 캘리브레이션을 대신한다.

### ★ 트림은 `ua[1]`(V_out) 기준으로 수행한다

트림은 BGR의 $R_2$를 조정해 $V_{ref}$를 비례 스케일한다. 루프 평형에서

$$V_{out} = \frac{V_{ref} - V_{os}}{\beta}$$

이므로, 특정 칩의 오프셋 $V_{os}$와 분압비 $\beta$가 무엇이든 **그 칩에서
$V_{out}$이 1.800 V가 되도록 $V_{ref}$를 고르면** 세 오차원이 모두 흡수된다:
BGR 공정 산포(원래 목적), 분압비 미스매치, EA 입력 오프셋.

| 트림 기준 | 관측 핀 | 잔여 오차 (MC-A 기반) |
| :--- | :--- | ---: |
| $V_{ref}$ = 1.200 V | `ua[0]` | ±21.73 mV |
| **$V_{out}$ = 1.800 V** | **`ua[1]`** | **±8.01 mV (양자화만)** |

**2.71배 개선이며 하드웨어 변경이 전혀 없다.** 측정 절차만 바꾸면 된다.

부작용 점검:

| 항목 | 영향 |
| :--- | :--- |
| $V_{ref}$가 1.2 V에서 이탈 | ±1.12% (±13.4 mV). $V_{ICM}$ 헤드룸(PMOS 708 mV)에 무해 |
| BGR TC | $R_2$ 비례 스케일이라 ppm/°C 보존 (`01_BGR_최종스펙.md` §6.3) |
| 트림 범위 소요 | BGR ±6.6%에 LDO ±1.12% RSS 합성 → ±6.69%. 0.09%만 증가 |
| 온도 드리프트 | 트림은 1회성 DC 보정. 분압비는 동일 재질이라 비 드리프트 무시, EA 오프셋 드리프트만 잔존(작음) |

`ua[0]`(V_ref)은 교차검증용으로 유지한다.

### 5.3. 노이즈 항의 지위
§1의 20~30 µV/√Hz는 **목표가 아니라 구조적 예측**이다.
레퍼런스 실측이 보여준 구조는 "LDO 자체 1.8~1.9 µV/√Hz vs 밴드갭 13~19 µV/√Hz × 이득 1.5"이며,
**LDO EA를 아무리 조용히 만들어도 BGR × 1.5가 바닥**이라는 뜻이다.
절대치는 우리 BGR 노이즈 측정(PEX 후) 이후 확정한다. **PASS/FAIL 판정 대상이 아니다.**

---

## 6. TinyTapeout 제약 (공식 문서 확인분)

| 항목 | 값 |
| :--- | :--- |
| 전원 핀 | VGND / **VDPWR = 1.8 V 디지털 코어** / **VAPWR = 3.3 V 아날로그**(선택) |
| 3.3 V 사용 시 | `tt_analog_*_3v3.def` 템플릿 필수 + `info.yaml`에 `uses_3v3: true` |
| PDN | **20 mA 소모 시 0.1 V 강하** → 우리 2.10 mA면 VAPWR ~10 mV 처짐 |
| 아날로그 핀 경로 | 저항 < 500 Ω, 커패시턴스 < 5 pF, 최대 전류 4 mA |
| 아날로그 핀 | `ua[0]`~`ua[5]`만, **0번부터 순서대로** |
| 디지털 출력 패드 | `sky130_ef_io_gpiov2_pad`, **최대 출력 33 MHz** (입력 66 MHz) |
| 금지 | **metal 5 사용 불가** |
| 필수 | 미구동 `uo_out`/`uio_out`/`uio_oe`는 GND 연결 |
| 면적 | 1x2 = 160 × 225 µm (3v3는 약간 좁음 — **DIEAREA로 확정 필요**) |

**귀결**: 디지털 도메인은 **1.8 V 고정**. 보드에서 3.3 V를 인가해도 TT 내부 로직(VDPWR)을 거쳐
오므로 우리에게 도착하는 건 1.8 V CMOS다. 이 때문에 트림 스위치를 **R2 접지단**에 배치해
1.8 V 직결로 구동하며, **레벨시프터가 불필요**하다.

### 6.1. ★ power-good 급전 레일 — VAPWR / thick-ox 확정

PG는 $V_{LDO}$가 아직 낮은 기동 구간부터 유효해야 하는데, $V_{LDO}$로 자신을 급전하면
그 구간에 비교기 자체가 죽어 출력이 부정(不定)이 된다 — PG의 존재 이유가 무너진다.
→ **VAPWR(3.3 V) 급전, thick-ox 비교기.**

thin-ox(`pfet_01v8`)로 이 회로를 만들 수 없는 것은 아니지만 헤드룸이 나쁘다 —
$L=2\,\mu\text{m}$에서 $|V_{th}|$가 **1.047 V**로 thick-ox pfet(1.0243 V)을 오히려
넘는다(RSCE, `02_LUT_앵커.md` §1.4). 1.8 V 레일에서 장채널 PMOS를 아날로그로 쓰면
$V_{ov}$가 0.75 V밖에 남지 않는다.

**레벨은 버퍼로 처리한다** — thick-ox 인버터를 VDPWR(1.8 V)로 급전하면 3.3 V 입력을
받아 1.8 V로 내보내고(thick-ox 게이트는 3.3 V 정격 안), 그 뒤에 필요하면 thin-ox
버퍼를 추가한다. 레벨시프터가 아니라 **전원 도메인이 다른 인버터 체인**이다.

---

## 7. 레퍼런스 프로젝트 (조건 대조용)

| 분류 | 예 | 쓸 것 | 쓰면 안 되는 것 |
| :--- | :--- | :--- | :--- |
| **동일 환경 실리콘** | devstdin TT08, cw_vref | **달성 가능치의 현실 기준** | — |
| **상용 데이터시트** | TI TLV713 급 | **스펙 항목·측정 조건의 어휘** | 목표 수치 (전용공정 + ATE 개별 트림 산물) |
| **우리 BGR 실측** | $V_{ref}$ 1.18659 V, TC 7.5 ppm | **이미 확정된 바닥** | — |

**devstdin TT08** (`tt08-ldo-bg-ref-osc`, 로컬에 클론 있음)
- 무부하 1.842 V @20 °C. 50 °C 구간 드리프트 10 mV인데 typical 시뮬은 2 mV만 설명
  → **MC 코너로만 해명됨. PEX + MC 필수**
- 출력 임피던스 실측 160 Ω = 대부분 TT 경로 (§5.1의 근거)
- 노이즈 구조 (§5.3의 근거)
- RO 5단 current-starved, 6.25 MHz. 기준전류 편차 영향을 평가하지 못해
  시뮬 8.1~10.8 MHz vs 실측 5.2~6.25 MHz로 어긋남

**cw_vref (ttsky25a)** — 측정 아키텍처 참고. 8-bit 트림, 테스트 버퍼 + loop-through 캘리브레이션
